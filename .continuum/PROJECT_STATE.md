# Cladium compact project state

Updated: 2026-08-31
Architecture: Version 2  
Phase: Runbook Phases 0–2 complete (Steps 1–12); Phase 3 done for now (Steps 13–17 complete, Step 18 explicitly deferred — D-022); Phase 4 complete (Steps 19–25); Phase 5 (text concierge) complete (Steps 26–29); Phase 6 (Vapi bilingual browser voice) done for now (Steps 30–33 complete, Step 34 explicitly deferred — D-038); Phase 7 (WhatsApp and Meta) complete (Steps 35–38); Phase 8 (full hardening) complete (Steps 39–42); Phase 9 (staging, UAT, production) underway — Steps 43–45 complete, Step 45's decision was **NO-GO** (D-049); Step 46 blocked until a fresh GO
Application code: scaffolded (Next.js App Router, TypeScript strict) with CI gates, shared platform primitives, locale routing, Day/Night theming, a public site shell, a Visit page, a menu-browsing UI (honestly unpublished live), a full provider-neutral domain/repository layer for takeaway/booking/event requests (D-023) — now proven race-free under genuine concurrency, not just sequential calls (D-025), a CSRF/session-guarded takeaway cart/review/submit API (D-024, no UI yet — nothing published to add to a cart), a complete booking/treehouse request flow with both API and a live `/book` UI page (D-026), a complete event/birthday enquiry flow with both API and a live `/event` UI page surfacing the approved décor/cake/outside-food wording (D-027), a protected staff workspace (`/staff`) with role-scoped queues/detail/transition/assignment for all three request types, backed by a dev-only sign-in seam pending real Supabase Auth (D-028), a concurrency-safe outbox dispatcher with retry/backoff/terminal-failure handling draining a single shared outbox into a pollable staff-notification store, with Supabase Realtime itself deferred (D-029), a compact cached concierge system policy plus four strict read tools (menu/venue-info/cart/request-status) reusing existing domain services exactly (D-030), a real, typechecked Anthropic Messages API orchestrator (`POST /api/concierge/chat`) — provider-neutral adapter, bounded tool/token/time loop, safe fallback — installed and wired but never live-called (no `ANTHROPIC_API_KEY` in this sandbox — D-031), a real concierge chat UI (`/concierge`) with two prepare-only draft tools (booking/event) — no submit tool is ever registered, so the model is structurally unable to write; a guest confirms through the exact same `/api/{bookings,events}/submit` endpoints the manual forms use (D-032), a versioned agent evaluation suite (`modules/evals/`) splitting deterministic orchestrator/policy guarantees (run and CI-gated now, 15 cases) from genuine model-judgment cases (fully specified, visibly skipped without a live API key, 12 cases) across all 11 runbook-named categories (D-033), controlled Vapi assistant configuration templates (`modules/voice/profiles/`) — a six-cell (environment × locale) matrix with no assistant id/credential field at all (structural, same pattern as Step 28's absent submit tool), a voice system prompt that wraps `CONCIERGE_SYSTEM_POLICY` verbatim rather than duplicating it, drift-detected fingerprint versions tying it to the text concierge's policy/tool schema, and provider/voice/transcriber selection deliberately left `PENDING_BAKEOFF` until Step 34 (D-034), a real, typechecked `POST /api/vapi/token` short-lived browser token service — a hand-rolled RFC 7519 HS256 JWT signer (`lib/security/jwt.ts`) plus a provider-neutral Vapi token issuer (`modules/integrations/vapi-client.ts`), gated by feature-flag/session/CSRF/origin/rate-limit checks (the first route in this codebase to actually read `FEATURE_VOICE_EN`/`FEATURE_VOICE_UR`), never live-called in this sandbox — no `VAPI_PRIVATE_KEY` (D-035), authenticated Vapi tool/webhook routes (`POST /api/vapi/tools`, `POST /api/vapi/webhook`) reusing Step 12's HMAC-SHA256/timestamp/replay verification unchanged, `toolCallId`-idempotent (Step 19's `runIdempotent`, a new scope), bounded (call-count cap + per-call timeout race), and dispatching through the exact same `modules/concierge/tool-registry.ts` text chat uses — never a second tool implementation (D-036), a real voice web experience (`/concierge`'s Type/Talk toggle, flag-gated server-side) built on the real `@vapi-ai/web` SDK — a pure, fully-tested call-state reducer/error-classifier (`voice-call-state.ts`) driven by real, SDK-type-verified event names, a server-side pending-confirmation bridge (`GET /api/vapi/pending-confirmation`) so a live call's server-executed draft becomes visible to the browser, and a review card shared byte-for-byte with text chat (`pending-confirmation.tsx`, extracted from Step 28's `concierge-chat.tsx`) — voice never submits anything itself (D-037), a hardened click-to-WhatsApp handoff (`lib/business/whatsapp-link.ts#buildWhatsAppUrl`) whose signature takes only a `Locale` — never guest data — so its minimal, reviewed, bilingual prefilled `?text=` message is structurally incapable of leaking PII, plus a visible external-navigation notice on every WhatsApp link and bilingual staff-escalation copy in the concierge's own safe fallback (D-039), and real consent and privacy controls (`modules/consent/`) — an append-only `ConsentEvent` ledger (`lib/domain/consent-event.ts`) mirroring `status_events`/`audit_events` exactly, four distinct grant/revoke categories (`ESSENTIAL_PREFERENCES` default-granted so locale/theme keep working with zero guest action, `META_MARKETING`/`MICROPHONE`/`RECORDING` all fail-closed by default), a versioned-policy staleness check, a real deletion/retention job backed by a new migration that gives `consent_events` its one narrow, auditable exception to an otherwise-unconditional append-only trigger, a working `/privacy` settings page (honest "not published yet" notice, never an invented legal page, alongside fully-functional consent controls), genuine server-side enforcement — `POST /api/vapi/token` now rejects with `CONSENT_REQUIRED` when MICROPHONE consent isn't granted, not just a client-side hidden button (D-040), and consent-gated Meta event tracking (`modules/integrations/meta-events.ts#trackMetaEvent`) — a closed, custom seven-event catalog matching `production-architecture-v2.md` §11 verbatim (never Meta's own `Purchase`/`Schedule` standard events), one shared flag-then-consent decision point reused by every trigger, a bounded-timeout best-effort CAPI adapter that can never block the guest-facing request that triggered it, wired into `add_to_cart`/`submit_order_request`/`submit_booking_request`/`submit_event_request`/`view_menu` with real trigger points, `contact`/`lead` fully built but deliberately not yet wired into the existing WhatsApp links, and the actual browser Pixel `<script>`/CSP change deliberately left as a scoped follow-up (D-041), and WhatsApp Cloud readiness (`cladium-research/operations/whatsapp-cloud-readiness.md`) — a real (never-called) outbound adapter matching Meta's documented template-message contract, a real signature/handshake verifier confirmed against Meta's actual two-secret webhook scheme (`X-Hub-Signature-256` plus a separate verify-token handshake), and one webhook route that checks `FEATURE_WHATSAPP_CLOUD` before anything else — off means `404`, the step's own evidence bullet, with a small local fix so an unrelated missing flag elsewhere can never turn that into a `500` (D-042). **Phase 7 (WhatsApp and Meta) is complete.**
Baseline commit: `8732db0` "chore: establish Cladium pre-build baseline" (local only, not pushed). Steps 8–10 committed at `150db60` (local only, not pushed). Steps 11–13 committed at `ec45245` (local only, not pushed). Step 14 committed at `84e37db` (local only, not pushed). Step 15 committed at `443cd0e` (local only, not pushed). Step 16 committed at `d6eceb9` (local only, not pushed). Step 17 committed at `3b5a58c` (local only, not pushed). Step 18 deferral documented at `3976cf6` (local only, not pushed). Step 19 committed at `93f4e33` (local only, not pushed). Step 20 committed at `78dbfcf` "feat: takeaway draft and review API (Step 20)" (local only, not pushed). Step 21 committed at `c123ce1` "fix: real concurrency race in idempotency/confirmation-token stores (Step 21)" (local only, not pushed). Step 22 committed at `e655b26` "feat: booking and treehouse request flow (Step 22)" (local only, not pushed). Step 23 committed at `198dcc0` "feat: birthday and event enquiry flow (Step 23)" (local only, not pushed). Step 24 committed at `8ffc860` "feat: protected staff workspace (Step 24)" (local only, not pushed). Step 25 committed at `0b83c8a` "feat: outbox dispatcher and staff notifications (Step 25)" (local only, not pushed). Step 26 committed at `90b8a4e` "feat: concierge policy and read tools (Step 26)" (local only, not pushed). Step 27 committed at `86a09fa` "feat: server-side chat orchestration (Step 27)" (local only, not pushed). Step 28 committed at `9ca7cc4` "feat: concierge draft actions and confirmation (Step 28)" (local only, not pushed). Step 29 committed at `9d31861` "feat: agent evaluation harness (Step 29)" (local only, not pushed). Step 30 committed at `67b38c9` "feat: Vapi configuration as controlled artifacts (Step 30)" (local only, not pushed). Step 31 committed at `7a2b087` "feat: short-lived Vapi browser token service (Step 31)" (local only, not pushed). Step 32 committed at `d9a5976` "feat: authenticated Vapi tools and webhooks (Step 32)" (local only, not pushed). Step 33 committed at `8446564` "feat: voice web experience (Step 33)" (local only, not pushed). Step 34 deferral committed at `5abc346` "docs: voice quality bake-off plan, explicitly deferred (Step 34)" (local only, not pushed). Step 35 committed at `1a7b9dc` "feat: click-to-WhatsApp handoff hardening (Step 35)" (local only, not pushed). Step 36 committed at `76b1f14` "feat: consent and privacy controls (Step 36)" (local only, not pushed). Step 37 committed at `5a1ae15` "feat: Meta measurement behind a flag (Step 37)" (local only, not pushed). Step 38 committed at `3fcc277` "feat: WhatsApp Cloud readiness (Step 38)" (local only, not pushed). Step 39 committed at `2c5be22` "test: cross-product E2E and accessibility (Step 39)" (local only, not pushed). Step 40 committed at `2cc0cb8` "fix: security and abuse verification — rate limiting and security headers (Step 40)" (local only, not pushed). Step 41 committed at `08ce57b` "test: performance and resilience load testing (Step 41)" (local only, not pushed). Step 42 committed at `5a30ba8` "fix: backup, restore, and rollback drill (Step 42)" (local only, not pushed). Steps up to and including Step 42 (`428f105`) pushed to GitHub (`aamirshah313-cyber/cladium_cafe`) and deployed live to a real Vercel staging environment (`https://cladium-cafe.vercel.app`) for Step 43. Step 43 committed at `c6e1ea1` "docs: staging release (Step 43)" (no application source changed — report and `.continuum/` only). Step 43's staging test-data check committed at `3c7be44` "chore: confirm Step 43 staging test data already cleared" (no application source changed). Step 44 committed at `ce91b3e` "docs: owner and staff UAT (Step 44)" (no application source changed — report and `.continuum/` only). Step 44's commit-hash reference committed at `7c31fce` (no application source changed). Step 45 committed at `4e7f0d8` "docs: production readiness decision - NO-GO (Step 45)" (no application source changed — report and `.continuum/` only). Real staff auth (D-050) committed at `43e10cc` "feat: real staff authentication with enforced owner/manager MFA (D-050)".

## Progress (step-completion metrics, not effort estimates)

- Overall: 43/47 runbook steps ≈ 91.5% (Steps 18 and 34 explicitly deferred, not counted as complete — see D-022, D-038). Step completion counts that the go/no-go report was produced, not that the answer was GO — see D-049.
- Phase 0 (governance/evidence, steps 1–3): 3/3 = 100%
- Phase 1 (repo/app foundation, steps 4–6): 3/3 = 100%
- Phase 2 (data/auth/security, steps 7–12): 6/6 = 100%
- Phase 3 (bilingual/dual-theme public experience, steps 13–18): 5/6 = 83.3% (1 deferred, not outstanding-forgotten)
- Phase 4 (deterministic requests and staff workflows, steps 19–25): 7/7 = 100%
- Phase 5 (text concierge, steps 26–29): 4/4 = 100%
- Phase 6 (Vapi bilingual browser voice, steps 30–34): 4/5 = 80% (1 deferred, not outstanding-forgotten)
- Phase 7 (WhatsApp and Meta, steps 35–38): 4/4 = 100% — complete
- Phase 8 (full hardening, steps 39–42): 4/4 = 100% — complete
- Phase 9 (staging, UAT, and production, steps 43–47): 3/5 = 60% (Step 45's decision was NO-GO; Step 46 blocked until a fresh GO)
- Deployment is step 46.
- Every step counts equally regardless of size/duration. See `CLAUDE.md` Workflow rules for the reporting rule.

## Goal

Build a luxury, mobile-first Cladium Café & Resort web application on Next.js/Vercel Pro with Supabase Pro, request-based takeaway/table/treehouse/event workflows, protected staff operations, English/Urdu text and browser voice concierge, Urdu RTL, Day/Night themes, click-to-WhatsApp, and consent-gated Meta support.

## Current truth

- Open 12 pm–12 am; takeaway yes; home delivery no.
- Requests are pending until staff acts. Never promise instant order, availability, booking, payment, or confirmation.
- Treehouses are limited/staff-confirmed.
- Birthday décor starts PKR 8,000; no cakes are provided; outside food is not allowed.
- Menu source (`data/menu.json`) verified by `scripts/validate/run-all.mjs`: 118 items across 12 categories, 100 single-price, 18 variant-price, zero missing prices, eight source pages, zero empty categories. (The Step 1 audit's "52 items / 4 empty categories" figure was a bug in that step's ad hoc script, which missed items nested under `groups[].items` in Steaks/Burgers/Bar Menu/BBQ — see D-015. The original 118-item figure was correct.) Owner publication signoff remains required.
- Vapi browser voice uses separate English/Urdu assistants, short-lived origin/assistant-restricted JWTs, HMAC/replay protection, and visible tap confirmation. Recording is off.

## Completed

- Production architecture v2 and data model v2.
- Mandatory production release gates.
- Claude Code master prompt v2 and 47-step gated runbook.
- Legacy contradictions corrected or marked historical.
- Start-here handoff and structural/data validation.
- Local token-efficient context/checkpoint layer added because the supplied CONTINUUM GitHub repository is not publicly accessible.
- Runbook Step 1 baseline audit complete: confirmed business facts, source-of-truth precedence, media inventory, gate risk register recorded.
- Runbook Step 2 ADRs recorded at `cladium-research/architecture/adr/0001`–`0008` (stack, request-only scope, bilingual routing, Day/Night tokens, server-side Anthropic tools, Vapi JWT/HMAC, transactional outbox, provider adapters).
- Runbook Step 3 deterministic, dependency-free validation tooling added at `scripts/validate/` (`node --test "scripts/validate/**/*.test.mjs"` and `node scripts/validate/run-all.mjs`); current run is a full PASS with zero errors/warnings, and it corrected the Step 1 menu-count bug (see above and D-015). Generates `cladium-research/data/validation/owner-signoff-report.md`.
- Local baseline committed (`8732db0`): the full pre-build research pack, governance files, ADRs, and validator tooling — 66 files, no real secrets, `.env.example` placeholders only. Not pushed to any remote.
- Runbook Step 4 scaffold: Next.js 16.3.2 App Router, React 19.2.8, TypeScript 6.0.3 strict, npm (not pnpm — pnpm unavailable in this environment, deviating from ADR-0001's proposal; npm used instead, package-lock.json committed-ready), ESLint 9 (flat config, `eslint-config-next` + `eslint-config-prettier`), Prettier, Vitest, Zod-based client/server/feature-flag env schema at `src/lib/env.ts`, and `src/modules/*` boundary directories (business/menu/takeaway/bookings/events/concierge/voice/staff/integrations/consent) per `production-architecture-v2.md` §4. No business features, no locale routing, no API routes yet — those are later steps. Clean install, format check, lint, typecheck, unit tests (8/8), source validators, and `next build` all pass.

- Runbook Step 5 CI and repository hygiene: `.github/workflows/ci.yml` (Node 24) runs install integrity (`npm ci`), `npm ls`, format, lint, typecheck, unit tests, source validators, scripts tests, secret scan, production build, and a client-bundle leak check — the same chain as `npm run verify`, runnable locally. Added dependency-free `scripts/security/` scanner (12 patterns, never prints matched values) plus `CONTRIBUTING.md` (commands, branch-protection recommendations, dependency-update policy incl. the ESLint 9 pin rationale). `.gitignore` and placeholder-only `.env.example` reviewed and already safe — unchanged. CI consumes no repository secrets and builds without credentials by design.
- Runbook Step 6 shared platform primitives: strict shared request schemas and boundary parsing, typed `Result`/application errors, correlation IDs, redacted structured logging, safe JSON responses, and synthetic test fixtures. Client-safe environment handling is isolated in `src/lib/env.ts`; privileged environment handling is isolated in `src/lib/env.server.ts` and rejects browser imports at module load. Full 11-gate verification passes (102 unit tests), including validation, redaction, response-safety, and environment-boundary tests.
- Runbook Step 7 complete: local `supabase/config.toml`, migration workflow, generated-types workflow, isolated environment guidance, transaction-pooler/direct-connection invariants, and an offline CI check. **Local smoke passed**: `supabase db reset` → exit 0 ("Finished supabase db reset"), PostgreSQL 17.6 verified queryable, `public` schema empty (correct pre-Step-8). Docker here is 4 GB, too small for all 11 services (containers OOM-killed, `exit 137`); the reduced stack `supabase start -x studio,edge-runtime,realtime,storage,imgproxy,supavisor,mailpit,vector,logflare` is stable and sufficient for the migration workflow — documented in `docs/database-environments.md`, not worked around in committed config. Fixed the `[inbucket]`→`[local_smtp]` deprecation. No hosted project connected; stack stopped afterwards.

- Runbook Step 8 core content schema: 4 migrations (`20260824120001`–`4`) creating 12 tables — business settings/hours/exceptions, feature flags, menu versions/categories/items/variants, translations, media assets, pricing rules, promotions. Integer-PKR money, `timestamptz` UTC, `version` + `updated_at` trigger on every row, tri-state availability defaulting to UNKNOWN, composite FKs so items/variants cannot straddle menu versions, at most one published menu version, and approval-required constraints on publishing (menu version, media, promotions) and on active pricing rules / enabled flags / approved translations. RLS enabled on all 12 tables with no policies yet = default-deny; policies are Step 10. `pricing_rules` and `promotions` are intentionally empty. Generated types committed at `src/lib/db/database.types.ts`. Evidence: `db reset` applies all 4 cleanly from scratch, `npm run db:test:schema` passes (accepts valid content, rejects invalid prices/states), offline migration lint added to `npm run verify`.

- Runbook Step 9 workflow schema: 4 further migrations (`20260824130001`–`4`), 16 more tables — staff profiles/role memberships, customer sessions, carts/cart items, confirmation tokens, idempotency keys, takeaway requests + immutable snapshot items, booking requests, event requests, status events, audit events, outbox events, webhook events, consent events. The three state machines are enforced by database trigger as well as in code: guest submissions may only be created in their initial state, and only listed transitions are legal. Snapshot lines and the status/audit/consent ledgers are append-only by trigger. `total = subtotal + adjustments` and `line_total = unit_price × quantity` are check constraints. Confirmation tokens are terminal once used; idempotency scope and `(provider, provider_event_id)` are unique; an unverified webhook cannot be marked processed. `conversation_summaries` deliberately NOT created (optional, and unapproved). Deferred FKs from Step 8 to `staff_profiles` added. 28 tables total, all RLS-enabled (default-deny). Evidence: `db reset` applies all 8 migrations cleanly; `npm run db:test:schema` passes with ~40 assertions.
- Runbook Step 10 authorization: 3 further migrations (`20260824140001`–`3`) — `rls_helpers` (SECURITY DEFINER helpers: `current_customer_session_id`, `current_staff_id`, `is_staff`, `staff_has_role`, `is_owner_or_manager`, `is_published_menu_version`, avoiding RLS recursion on `staff_profiles`), `rls_policies` (every one of the 28 tables gets explicit policies: anonymous public-read of published/approved content only, guest access scoped to `app.customer_session_id` set per-transaction by the server, and five staff roles — OWNER, MANAGER, ORDER_STAFF, BOOKING_STAFF, AUDITOR — with least-privilege reads/writes; AUDITOR is read-only by omission), `grants` (table-level GRANTs layered under the policies; `confirmation_tokens`/`idempotency_keys` have no client grant at all — service_role only — and neither do `feature_flags`/`pricing_rules`/`audit_events`/`staff_profiles` for `anon`, which denies at the privilege layer rather than filtering rows). Added a `staff_requiring_mfa` view (owner/manager in scope) and a `public_business_settings` view for anonymous-safe settings. New allow/deny matrix at `scripts/db/rls-tests.sql` (`npm run db:test:rls`) exercises anon, two independent guests, all five staff roles, and service_role. New offline (no-Docker) migration lint at `scripts/db/migration-invariants.mjs` (filenames/ordering, every created table gets RLS enabled, no unacknowledged destructive DDL, money columns are integer, timestamps are `timestamptz`) wired into `npm run verify`. Evidence: `db reset` applies all 11 migrations cleanly; `npm run db:test` (schema + RLS) passes; full `npm run verify` passes (48 unit tests + build + scans). One test/grant mismatch was found and fixed in this session: `rls-tests.sql` originally expected `anon` to get an empty result set (RLS-level filtering) on tables that in fact have no GRANT to `anon` at all (`feature_flags`, `pricing_rules`, `confirmation_tokens`, `audit_events`, and `staff_profiles` as seen from a guest session) — Postgres correctly raises `insufficient_privilege` instead. Added a `pg_temp.expect_no_access()` test helper for that case; no migration/policy/grant changed, only the test's expectation.

- Runbook Step 11 menu normalization/import planning: preserved the source adapter and added pure draft import, owner diff-review, and publish-intent modules. The draft plan detects matching source checksums, keeps availability UNKNOWN, and creates human-review Urdu translation candidates; the publish plan enforces approved-reviewer fields and a single-published-version transition without database writes. Evidence: 18 focused tests and full `npm run verify` pass (120 application tests, 48 script tests, source validation, secret scan, production build, client-output scan). No menu was imported or published; persistence is later Step 19.
- Runbook Step 12 security foundation: server-only signed opaque session cookies, session-bound CSRF tokens and strict origin checks, safe redirects, JSON body limits, CSP/security headers, provider-neutral rate-limit/replay interfaces with explicit in-memory development adapters, and constant-time timestamped HMAC webhook verification added. Safe errors gained 413/415 handling; redaction coverage now includes PII, tokens, chat, and signatures. Evidence: 36 focused security tests cover forged origin/CSRF, unsafe redirects, oversized bodies, bad signatures, stale/replayed webhooks, rate-limit boundaries, headers, and redaction; full `npm run verify` passes (156 application tests, 48 script tests, source validation, secret scan, production build, client-output scan). No secrets or provider routes were added.
- Runbook Step 13 locale routing: `/en` and `/ur` server-rendered under `app/[locale]/`, with a `(root-fallback)` route group for the bare `/` path so it has its own `<html>`/`<body>` rather than sitting above `[locale]/layout.tsx` (D-018 — a shared root layout silently produced `lang="en"` on every Urdu route; caught by a live browser check, not by any automated gate). `src/proxy.ts` (Next.js 16's `proxy` convention, replacing deprecated `middleware.ts`, needed for `node:crypto`) negotiates a locale for `/` from a verified signed preference cookie first, then `Accept-Language`, then `DEFAULT_LOCALE` — `negotiateLocale` only ever returns a closed-set locale, so this can never open-redirect. `GET /api/locale-preference` is a plain, no-JS link target that sets the signed cookie and redirects, used by the server-rendered language switcher. `lang`/`dir`, canonical/hreflang/`x-default` metadata, and reviewed (non-menu) chrome copy are in place; only reviewed application strings are translated — no menu/business/policy text exists yet to mistranslate. Evidence: 21 new focused tests plus a live browser check of `/`, `/en`, `/ur`, and the switcher round-trip; full `npm run verify` passes.
- Runbook Step 14 Day/Night design system: semantic tokens from theme-mode.md in `globals.css` (Day unconditional on `:root`; Night applied both as a `prefers-color-scheme` first-visit default, guarded against an explicit Day choice, and as an explicit `data-theme="night"` override that always wins). A labelled, keyboard-accessible `Day | Night` button group (`app/[locale]/theme-toggle.tsx`, never icon-only) switches instantly client-side (no navigation — route, locale, and any request/cart state untouched) and persists via a deliberately unsigned, isomorphic cookie (`lib/theme/`, D-019 — theme has no security/routing consequence, unlike locale, and the cookie must be client-JS-readable for an instant switch). `app/[locale]/layout.tsx` reads that cookie server-side to set the initial `data-theme` before first paint (no flash); this makes `/en`/`/ur` dynamically rendered rather than statically prerendered (D-019). Colour transitions are 180ms and guarded by `prefers-reduced-motion`; focus is visible via `:focus-visible`. Evidence: 13 new focused tests, including a dependency-free scan of `globals.css` confirming every Day/Night token is present with the exact theme-mode.md hex values in all three CSS blocks; full `npm run verify` passes; live browser check confirmed correct `data-theme`/colours on fresh loads of `/en` and `/ur` in both themes, correct cookie persistence, and correct instant switching (verified with the CSS transition temporarily disabled, because the test harness's hidden preview pane doesn't produce compositor frames to drive a live transition — a tooling artifact, not an app bug).
- Runbook Step 15 public shell and navigation: `SiteHeader` (skip link, brand link, primary `nav` — currently just Home, since Menu/Visit/Concierge don't exist yet and "hide unavailable routes" is this step's own instruction — plus the language/theme utility controls) and `SiteFooter` (brand name, hours "12 pm – 12 am" preserved un-translated per the numerals/configured-values rule, and a live "Open now"/"Closed now" status computed timezone-aware in `Asia/Karachi` via `lib/business/hours.ts`, no invented exceptions). Address/map/WhatsApp contact deliberately deferred to Step 16. Loading (`[locale]/loading.tsx`), route-level not-found (`[locale]/not-found.tsx`), and a client error boundary (`[locale]/error.tsx`, locale derived from `usePathname()` since `error.js` gets no route params, `error.message` never rendered) all render inside the correctly localized shell. A root `app/not-found.tsx` and `app/global-error.tsx` are self-sufficient (own `<html>`/`<body>`, bilingual) fallbacks for failures above the locale layout. A catch-all `app/[locale]/[...rest]/page.tsx` was added after a live browser check found `/en/nonexistent` — a path with no matching route at all — skipped the nested not-found entirely and fell through to the bare root fallback, losing `lang`/`dir`/theme/header/footer (D-020). Evidence: 21 new focused tests (business-hours timezone correctness, `localeFromPathname`); full `npm run verify` passes; live browser check of header/nav/footer/hours-status in both locales and themes, both 404 paths (invalid locale vs. valid-locale-unmatched-path), the error boundary (verified via a temporary throwing test page, then removed), and no horizontal overflow at 360px in both LTR and RTL.

- Runbook Step 16 home, location, and contact: `modules/business/facts.ts` expanded with address, Google Maps URL, WhatsApp number/URL, and policy prose (directions, seating, delivery, birthday/décor, cakes, outside food) — every value transcribed from `business-profile.json`/`approved-operations-knowledge.md`, policy prose wrapped as `LocalizedText` via `canonicalLocalizedText` (renders English in both locales until an owner approves Urdu — none is yet). New `/visit` route (one page, matching site-map.md's single "Visit" node: directions/map, hours+live status, WhatsApp contact, a "Good to know" policy list) and a light truthful Home update (retained tagline, a "Plan your visit" link, a WhatsApp CTA) — no photography (none approved yet) and no menu teaser (Step 17, not built yet). Map/WhatsApp links are `target="_blank" rel="noopener noreferrer"`. `SiteHeader`'s primary nav gained a real active-state indicator (`primary-nav.tsx`, a small client island using `usePathname()`, now that Visit is a second real destination — Step 15's single-item placeholder wasn't worth that investment yet). Evidence: 25 new focused tests (exact-value transcription checks, no-invented-Urdu checks, a source-scan confirming every external link carries `rel="noopener noreferrer"`); full `npm run verify` passes; live browser check of the Visit page and updated Home in both locales/themes, correct nav active-state, correct external-link attributes, and no horizontal overflow at 360px.

- Runbook Step 17 accessible menu browsing: `modules/menu/menu-view.ts` defines the guest-facing `PublishedMenuView` shape (categories → items → variants, tri-state availability, integer PKR) and `getPublishedMenuView()`, which always returns `UNPUBLISHED` today — deliberately, per D-021 (user-confirmed): the owner sign-off gates (release-gates-v2.md Gate 0/Gate 2, D-005) aren't satisfied yet, so the live `/menu` route shows an honest "not available online yet" state with WhatsApp/Visit fallbacks rather than the real, unapproved 118 items. The full search/category-filter/availability/PKR-formatting rendering UI (`menu/page.tsx`) is completely built — a plain `<form method="GET">` reading `searchParams` server-side, so it works with no JavaScript at all — and was verified live against a temporary fixture (reverted before commit) covering both locales, all three availability states, no-results, and no-JS query-param filtering. `formatPkr` (`lib/business/money.ts`) matches the "PKR 8,000" style already used in approved copy. Menu now appears in primary nav (a real, working route, just without content yet). Evidence: 12 new focused tests (`filterMenuCategories` logic, `formatPkr`, a tripwire asserting `getPublishedMenuView()` stays `UNPUBLISHED`); full `npm run verify` passes; live browser check as described above, plus no horizontal overflow at 360px.

- Runbook Step 18 menu carousel: explicitly deferred, not built (D-022, user-confirmed). Both of the step's own stated preconditions are unmet — the menu is `UNPUBLISHED` (Step 17/D-021) and zero approved photos exist — so this is a documented, deliberate skip, not an oversight. Revisit once the menu is actually published and a photo/media mapping is owner-approved.

- Runbook Step 19 domain repositories and state machines (D-023): `lib/domain/` — provider-neutral, dependency-injected primitives shared across all three entities: `actor.ts` (Actor/StaffRole/`hasAnyRole`), `state-machine.ts` (generic `canTransition`/`isTerminal`), `staff-transition.ts` (the one `performStaffTransition` orchestrator implementing data-model-v2.md §7's staff-transition contract exactly — authorize, lock+version-check, validate transition, update, append status+audit events, optional outbox notification), `idempotency.ts` (`runIdempotent` — same key+fingerprint replays without re-running; different fingerprint or an in-flight duplicate is rejected), `confirmation-token.ts` (single-use, only a SHA-256 hash stored, a review-hash mismatch is `STALE_REVIEW`), `review-hash.ts`, `status-event.ts`/`audit-event.ts`/`outbox.ts` (append-only builders), `versioned-store.ts` and `sink.ts` (in-memory reference stores — no live Postgres adapter yet, see D-023 for why). `modules/{takeaway,bookings,events}/state-machine.ts` encode data-model-v2.md §5/tool-contracts.md's three diagrams exactly, including which actor type may perform each transition (AUDITOR excluded everywhere; event's `QUOTED → CUSTOMER_ACCEPTED` is guest-performed, uniquely, but has no service yet — no tool contract requests it). `modules/takeaway/cart.ts` (add/modify/remove/recompute against a `PublishedMenuView`, blocking only confirmed-`UNAVAILABLE` items) and `modules/{takeaway,bookings,events}/submission-service.ts` (`prepare*`/`submit*`, implementing the full §7 submission transaction contract step-by-step) are the concrete per-entity services. Evidence: 292 new focused tests — every state×state pair for all three machines asserted against an independently-written expectation table; idempotency replay/conflict/concurrent-duplicate/retry-after-failure; confirmation-token issue/single-use/session-and-action-mismatch/expiry/stale-review; optimistic-lock races; full submission-service happy paths, idempotent replay, and stale-review for takeaway/booking/event. Full `npm run verify` passes (526 tests total, production build unaffected — nothing here is wired to a route yet).

- Runbook Step 20 takeaway draft and review (D-024): `lib/customer-session.ts` (first real use of Step 12's guest session cookie), `lib/http/{session-route,mutating-route,respond}.ts` (session resolution; a deterministic CSRF token — `HMAC(sessionId, SESSION_SECRET)`, no server-side token storage — plus origin check, both failing *closed* if `SESSION_SECRET`/`NEXT_PUBLIC_APP_URL` aren't configured; content-type/body-size checks; Result→`NextResponse` mapping), `modules/takeaway/{cart-store,http,deps,schemas}.ts` (in-memory session-keyed cart store; pure route orchestration; the process-lifetime deps singleton; zod body schemas). Five routes: `GET/POST /api/takeaway/cart[/items[/:cartLineId]]`, `POST /api/takeaway/review`, `POST /api/takeaway/submit`. Also added `lib/env.ts`'s `parseAppUrl` (narrow accessor, same pattern as `parseSessionSecret`) after its own test caught a real bug: the origin check originally required the *entire* client env schema, so it failed closed even for a valid CSRF token whenever the unrelated Supabase env vars were unset — exactly this environment's actual state. Cross-session access, price tampering, invalid quantity, and stale-menu are all structural (never a forgettable runtime check) and covered by tests; no cart/review UI page is built yet — the menu is still unpublished (D-021), so there is nothing for an "Add to cart" control to add (D-024). Evidence: 32 new focused tests; full `npm run verify` passes (558 tests total, all 5 routes appear in the production build); live check confirmed the deployed route fails closed safely with `SESSION_SECRET` unconfigured, and no regression on `/menu`, `/visit`, `/en`.

- Runbook Step 21 takeaway submission (D-025): review-hash token issuance, idempotent transactional submission to `REQUESTED` with snapshot lines/status/audit/outbox, and "repeated identical submission replays, changed payload is rejected" were already fully built in Steps 19–20. What Step 21's own evidence bullet asks for beyond that — "concurrency, double-click, retry, rollback, and atomicity" — is a *different* test shape (genuinely concurrent `Promise.all` calls, not sequential ones), and checking it directly found a real bug: `lib/domain/idempotency.ts` and `lib/domain/confirmation-token.ts` each did a two-step "read, decide, write" that let two truly concurrent callers both pass the "not started"/"not used" check before either wrote back. Fixed by replacing each with one atomic store operation (`findOrBegin`, `claimIfUnused`) that a real adapter must implement as a single conditional write. `versioned-store.ts`'s optimistic lock was already correct by the same reasoning (no internal `await` to interleave around). True multi-write atomicity/rollback across several in-memory stores is deferred to the real Postgres adapter (D-023) — in-memory writes can't fail, so there's nothing to roll back yet; what's provable now is that `submission-service.ts` already does all validation before any write. Evidence: 5 new focused `Promise.all` tests (a genuine double-click on the same idempotency key/confirmation token resolves to exactly one success, never two and never zero; a `FAILED` record with a different fingerprint still blocks as a mismatch). 563 tests total; full `npm run verify` passes.

- Runbook Step 22 booking and treehouse requests (D-026): `modules/bookings/{deps,schemas,request-window}.ts`, `app/api/bookings/{review,submit}/route.ts`, and a new shared `GET /api/session/csrf` bootstrap complete the booking API, directly reusing Step 19's `submission-service.ts` and Step 20/21's session/CSRF/idempotency machinery (no new mechanism, only a second entity wired to it). Unlike takeaway (D-024), a real UI was also built: `app/[locale]/book/page.tsx` plus a client `booking-form.tsx` (form → server-echoed review → confirmed, three stages), reachable from primary nav (`navBookLabel`) and two Home CTAs ("Request a Table", "Request Treehouse Seating" via `?seating=treehouse`) — buildable now because, unlike takeaway's cart, booking has no dependency on the still-unpublished menu (D-021). `requestedDate`/`requestedTime` get dedicated regex schemas; "today or later" is an independently unit-tested pure function (`isTodayOrFutureDate`), not only an inline zod refinement. The confirmed-stage copy explicitly states "not yet a confirmed reservation" (data-model-v2.md §5). Evidence: 10 new focused tests (date-boundary + full booking submission-service happy-path/idempotent-replay/concurrency/bad-token, mirroring Step 21's `Promise.all` shape); 573 tests total; full `npm run verify` passes, including production build listing `/[locale]/book` and all three new routes. Live end-to-end happy-path check remains blocked by the same standing `SESSION_SECRET`/`.env.local` sandbox limitation as Steps 20–21.

- Runbook Step 23 birthday and event enquiries (D-027): `modules/events/{deps,schemas}.ts`, `app/api/events/{review,submit}/route.ts`, and `app/[locale]/event/page.tsx` + client `event-form.tsx` (same form → review → confirmed shape as booking's) complete the event flow on Step 19's already-built `modules/events/submission-service.ts`/state machine. `decorInterest` is only a boolean flag — `quotedAmountPkr` stays `null` at creation; only a staff `QUOTED`/`CONFIRMED` transition can ever set one, and none is built (no tool contract requests it, per Step 19). The runbook's own evidence bullet requires "approved décor/cake/outside-food wording," not just accepting the flag, so `event/page.tsx` renders `BIRTHDAY_POLICY_TEXT`/`CAKE_POLICY_TEXT`/`OUTSIDE_FOOD_POLICY_TEXT` (already-approved `LocalizedText`) above the form. Refactored `isTodayOrFutureDate` from `modules/bookings/request-window.ts` to `lib/business/request-window.ts` (test renamed, no behavior change) so events could reuse it without importing across a module boundary from bookings — matches production-architecture-v2.md §4's `lib/` = "shared primitives only". Added the CLAUDE.md-mandated "Plan a Birthday" CTA to nav and Home. Evidence: 5 new focused tests mirroring Step 22/21's exact concurrency-test shape (the underlying mechanism needs no re-derivation, only re-exercising for a second entity); 578 tests total; full `npm run verify` passes, including production build listing `/[locale]/event` and both new routes. Live end-to-end happy-path check remains blocked by the same standing sandbox limitation as Steps 20–22.

- Runbook Step 24 protected staff workspace (D-028): `lib/domain/staff-transition.ts` gained mandatory-reason enforcement (`reasonRequiredStates`) and a new `performStaffAssignment` orchestrator (metadata-only: authorized/version-locked/audited, no state-machine check, no status event, no outbox); `lib/domain/{versioned-store,sink}.ts` gained `list()`. `modules/{takeaway,bookings,events}/staff-service.ts` wrap those plus a filtered read side (`modules/staff/filter.ts`, safe in-memory substring matching) per entity, taking `deps` explicitly (same pattern as `submission-service.ts`) rather than reaching into the process singletons, so each is independently unit-testable. `app/staff/` is a third independent root layout (alongside `(root-fallback)`/`[locale]`, per production-architecture-v2.md §4) with a real, English-only, unindexed UI: sign-in, per-entity queues with state/search filters, and a detail view with transition/assignment controls and append-only history. Real staff auth (Supabase Auth + owner/manager MFA, `staff_profiles`) is explicitly deferred to Gate 3 — needs a live Supabase project this sandbox doesn't have (D-017), same reasoning as D-023. `modules/staff/dev-credentials.ts` + the optional `STAFF_DEV_ACCOUNTS` env var is a clearly-documented, fail-closed-by-default stand-in (empty/unset in every real environment). Roles are looked up fresh per request from `modules/staff/directory.ts`, not baked into the signed session token, so a role change/removal takes effect immediately; the staff cookie reuses Step 12/20's exact signed-token primitive under a distinct name (`lib/security/session.ts`'s new optional `baseName` param, backward-compatible) and — unlike the guest session — is never auto-minted. AUDITOR can view every queue but write nothing (tested per entity). Events' `REQUESTED → QUOTED` requires `quotedAmountPkr`, written atomically with the state change via a new `additionalPatch` field on `performStaffTransition`. Noticed but not fixed (out of scope, tracked in TASKS.md): `FEATURE_{TAKEAWAY,BOOKING,EVENT}_REQUESTS` flags exist but are never read by the guest-facing routes. Evidence: 95 new focused tests (mandatory reasons, `performStaffAssignment`'s full matrix, per-entity concurrent-transition races, AUDITOR view-only, events' quote requirement, staff session/cookie isolation, `resolveStaffActor`/`guardStaffMutation`, `parseStaffDevAccounts` fail-closed parsing, `VersionedStore.list()`); 658 tests total; full `npm run verify` passes, including production build listing all `/staff/*` pages and 13 new `/api/staff/*` routes, plus a client-bundle scan confirming no dev-account data reaches the browser. Live check: `/staff` renders sign-in; `/staff/takeaway` renders its filter UI and fails closed (safe generic 500, no crash) with `SESSION_SECRET` unconfigured; `/staff/bogus-entity` 404s; no regression on `/`, `/en`, `/book`, `/event`.

- Runbook Step 25 notifications and outbox dispatcher (D-029): `OutboxEvent` (`lib/domain/outbox.ts`) gained `id`/`version` — the first thing since Step 19 needing to address one specific outbox row. `lib/domain/outbox-store.ts`'s `claimBatch` atomically claims due-`PENDING`-or-stale-`CLAIMED` rows (the Step 21 lesson applied again: one conditional operation, never read-then-write); `lib/domain/outbox-dispatcher.ts`'s `runDispatchCycle` resolves every claim to `DELIVERED`, retried with bounded exponential backoff, or terminal (`FAILED`) at `maxAttempts` — never left decided-nothing. `outbox_events` is one table, not one per entity (data-model-v2.md §6), so `modules/{takeaway,bookings,events}/deps.ts` now all point `outbox` at one shared singleton (`modules/notifications/deps.ts`) instead of three private sinks, so one dispatcher drains all three. The only registered handler today, `modules/staff/notification-handlers.ts`'s `staff_notification` handler, upserts by outbox-event id into `modules/staff/notification-store.ts` — idempotent by construction, since the dispatcher may invoke a handler twice for the same event (an at-least-once-delivery ambiguity, not a bug). Supabase Realtime is explicitly deferred (D-017); `/staff` polls `GET /api/staff/notifications` instead — ADR-0007 itself frames Realtime as "a speed-up, not the delivery guarantee," so polling the durably-dispatched result already gives the actual guarantee. The trigger, `GET /api/cron/outbox-dispatch`, is authenticated via `Authorization: Bearer $CRON_SECRET` (`deployment-target.md`'s named pattern for "authenticated outbox/retention jobs"); added `parseCronSecret` (fails closed to `undefined`, never throws) and `lib/security/cron-auth.ts`'s constant-time `verifyCronAuthHeader`. Evidence: 34 new focused tests covering the runbook's own evidence bullet exactly — worker overlap, crash/retry with growing backoff, poison-message terminal handling (never retried again after `maxAttempts`), and terminal-failure visibility (`lastError`/`failedAt` queryable), plus idempotent-handler and cron-auth fail-closed tests; 684 tests total; full `npm run verify` passes, including production build listing `/api/cron/outbox-dispatch` and `/api/staff/notifications`. Live check: the cron endpoint 401s with a safe generic message and no `Authorization` header (matching this sandbox's unconfigured `CRON_SECRET`); `/staff` still renders sign-in correctly, no regression.

- Runbook Step 26 concierge policy and read tools (D-030): `modules/concierge/policy.ts`'s `CONCIERGE_SYSTEM_POLICY` — one compact, cached, rules-only constant (tool-first facts, never-confirm-instantly, bilingual-without-inventing-Urdu, WhatsApp escalation), built from the exact same `business/facts.ts` constants the guest-facing pages render, never a duplicate or freshly-authored copy. Four strict read tools (`modules/concierge/tools/`): `getMenu` reads `getPublishedMenuView()`/`filterMenuCategories` (still `UNPUBLISHED`, D-021); `getVenueInfo` reads `business/facts.ts` plus live `isOpenAt` over a closed topic enum; `viewCart` is a direct re-export of `modules/takeaway/http.ts`'s `getCart` (no second cart implementation, server-computed totals only); `getRequestStatus` checks all three request stores and returns `{found:false}` identically for "doesn't exist" and "belongs to someone else." `modules/concierge/schemas.ts`'s `viewCartInputSchema` is deliberately empty — the model supplies nothing; the server injects the session. Nothing here calls a live LLM: no `@anthropic-ai` SDK dependency exists yet and `ANTHROPIC_API_KEY` has never been read by any code path — that wiring is explicitly Step 27's job, and live-model evals are Step 29's; "evals" here are deterministic tests asserting every tool's output is byte-identical to its approved source constant. Evidence: 6 new focused test files (strict-schema contract tests for all four inputs; policy content/compactness; `getMenu`'s tripwire plus full filtering/lookup against an injected fixture; `getVenueInfo`'s exact-match-per-topic plus live hours plus no-invented-Urdu; `viewCart`'s cross-session isolation; `getRequestStatus`'s three-store ownership-scoping matrix). 737 tests total; full `npm run verify` passes; no new routes (this is a module layer Step 27 will call), so nothing new to build-list or live-verify this step.

- Runbook Step 27 server-side chat orchestration (D-031): installed `@anthropic-ai/sdk`. `modules/integrations/anthropic-client.ts`'s provider-neutral `ChatClient` interface + real (never-live-called, no API key) Anthropic adapter — construction never throws, the key is only read inside `sendMessage`. `modules/concierge/orchestrator.ts`'s `orchestrateTurn`: rate-limit (Step 12's limiter, unchanged) → bounded server-held history (`conversation-store.ts`, never browser-supplied) → a tool-call loop bounded by call count/cumulative tokens/wall-clock deadline → every failure mode (poison loop, budget breach, thrown exception) resolves to the same safe WhatsApp-pointing fallback, never a leaked exception. `system` is always exactly `CONCIERGE_SYSTEM_POLICY`, structurally immune to anything a guest types. `modules/concierge/tool-registry.ts` pairs each Step 26 tool with a hand-written strict JSON schema; an unknown tool name is an orchestrator-level `NOT_FOUND`, a schema-invalid input for a known tool is a self-correctable tool-result error the model can retry from. No chat UI built — deliberately deferred to Step 28, the natural home for CLAUDE.md's "Ask Cladium Concierge" CTA once the concierge can also propose requests. Evidence: 4 new test files (cross-session isolation, prompt-injection structural guarantees, loop exhaustion, timeout via injectable clock, PII-safe telemetry, cost-limit, plus content-translation round-trips and tool-registry validation) — exactly the runbook's own evidence bullet; 779 tests total; full `npm run verify` passes, including production build listing `/api/concierge/chat` and a client-bundle scan confirming no SDK/key material reaches the browser. Live check (after a one-time Turbopack new-directory route-discovery delay resolved by a dev-server restart, not a code issue) confirmed the endpoint fails closed with a safe generic 500, `SESSION_SECRET` unconfigured.

- Runbook Step 28 concierge draft actions and confirmation (D-032): `prepareBookingRequest`/`prepareEventRequest` concierge tools (`modules/concierge/tools/prepare-{booking,event}.ts`) are thin wrappers over Step 19's existing `prepareBookingRequest`/`prepareEventRequest` — no second review/token path — with input schemas reusing `modules/{bookings,events}/schemas.ts`'s exported draft-field validators exactly (same "not in the past" check, same bounds). No `prepareTakeawayRequest` yet — same D-021/D-024 menu-unpublished reasoning. The core guarantee: no submit tool is ever registered in `tool-registry.ts` — structurally, not just by convention — a model attempting one gets `NOT_FOUND` like any unknown tool; only a guest tapping the real chat-UI Confirm control calls `POST /api/{bookings,events}/submit` directly, the exact endpoints and confirmation-token/idempotency contract the manual forms already use. `orchestrateTurn` gained `pendingConfirmation`, surfacing a successful prepare call's review+token structurally (not only as model-phrased text) so the UI renders a real card; only the latest prepare call in a turn wins, and it survives even a later failure in the same turn. A real chat UI shipped this step (`/concierge`), the natural place for CLAUDE.md's "Ask Cladium Concierge" CTA now that there's something to help complete, not only answer questions about. Evidence: 4 test files new/extended (strict-schema contracts; an end-to-end proof a concierge-issued token really works through the real submit function, and that a post-review tamper still fails `STALE_REVIEW`; the full 6-tool registry list with no submit-tool name ever present; `pendingConfirmation` surfacing/survival/latest-wins). 805 tests total; full `npm run verify` passes, including production build listing `/[locale]/concierge` and `/api/concierge/chat`. Live check confirmed the concierge page renders correctly and fails closed the same way every guest route has since Step 20.

- Runbook Step 29 agent evaluation harness (D-033, Phase 5 complete): `modules/evals/{types,assertions,runner}.ts` run every case through the real `orchestrateTurn` (Step 27) against a scripted fake `ChatClient` — never a reimplementation. Every `EvalCase` declares `requiresLiveModel`: `false` cases run now, deterministically, with real tool dispatch (including the real Step 19 submission service), so assertions inspect genuine orchestrator output — e.g. `reviewNeverQuotesAPrice()` checks the actual prepared `EventReview` object, `systemPromptContains(...)` checks the actual `CONCIERGE_SYSTEM_POLICY` string sent, not case-authored text; `true` cases (bilingual comprehension, ambiguous tool selection, live injection resistance) cannot run without `ANTHROPIC_API_KEY` (D-031) and are visibly `skipped` with a reason, never faked. `modules/evals/cases/` covers all 11 runbook-named categories, 27 cases total (12 skipped, 15 run and pass). Every runnable (`requiresLiveModel: false`) case is `critical: true` — each encodes a structural `CLAUDE.md` guarantee. `tests/unit/evals.test.ts` is the CI gate (fails on any critical, non-skipped failure, plus suite data-integrity checks); `cladium-research/agent/eval-policy.md` documents the non-critical threshold and review process, and ties the still-open `requiresLiveModel: true` coverage to `release-gates-v2.md` Gate 5's pre-launch requirement (tracked in TASKS.md). Evidence: 6 new tests plus the suite itself; 810 tests total (66 files); full `npm run verify` passes. No new route; nothing new to live-verify in the browser this step.

- Runbook Step 30 Vapi configuration as controlled artifacts (D-034, Phase 6 underway): `modules/voice/profiles/{types,policy,templates,validate}.ts` — a `VAPI_PROFILE_MATRIX` with one `VapiAssistantProfile` per (environment × locale) cell (development/preview/production × en/ur, 6 cells). `types.ts` has no field for an assistant id, org id, or any credential at all — the same "structural absence, not convention" pattern Step 28 used for the missing submit tool — so a template can never carry an id across an environment boundary; real ids stay exclusively in per-environment server env vars (`VAPI_ASSISTANT_EN_ID`/`VAPI_ASSISTANT_UR_ID`/etc., declared since Steps 6/12), read only by the later token/tool routes (Steps 31–32), never this module. `policy.ts#buildVoiceSystemPrompt` wraps `CONCIERGE_SYSTEM_POLICY` (Step 26) verbatim plus a short voice-conduct addendum — never a second, independently-authored copy of the approved facts, satisfying `CLAUDE.md`'s "same approved knowledge... as text." `POLICY_VERSIONS`/`TOOL_SCHEMA_VERSIONS` are small fingerprint lockfiles implementing the runbook's own "shared policy/tool schema versions" wording literally: a test recomputes the live SHA-256 fingerprint of `CONCIERGE_SYSTEM_POLICY`/`TOOL_DEFINITIONS` and fails if a profile's recorded version no longer matches, catching future drift instead of discovering it live. `voiceStack` is a discriminated union defaulting every cell to `{ status: 'PENDING_BAKEOFF' }` — a real provider/voice/transcriber selection can only exist with the `evidenceRef` Step 34's bake-off produces. `firstMessage`/`closingMessage` are `canonicalLocalizedText` (render English in both locales) — deliberately stricter than `lib/i18n/chrome.ts`'s already-reviewed UI-copy precedent, since this becomes literal spoken audio no fluent-speaker/owner review has evaluated yet. `src/modules/voice/profiles/CHANGELOG.md` and `docs/vapi-deployment.md` (three-environment table, per-cell deployment procedure, development→preview→production promotion order) satisfy the runbook's "change logs, and a deployment procedure" wording. Evidence: 17 new focused tests (matrix completeness, recording-disabled, still-pending-bakeoff, no-invented-Urdu, no-credential-field, bounded call limits, fingerprint drift detection, validator-catches-real-breakage, Vapi vars absent from the client env schema); 827 tests total; full `npm run verify` passes. No new route (no live Vapi call exists yet); nothing to live-verify in the browser this step.

- Runbook Step 31 short-lived browser token service (D-035, Phase 6 underway): `lib/security/jwt.ts` — a hand-rolled, spec-shaped compact HS256 JWT signer/verifier (`node:crypto`, no new dependency, same reasoning as `security/session.ts`/`security/webhook.ts`) — real RFC 7519 wire format, since the token this signs is handed to a third party (Vapi) that verifies it independently, unlike `session.ts`'s internal-only cookie format. `modules/integrations/vapi-client.ts#createVapiTokenIssuer` never throws at construction (same "fail only when actually used" shape as `createAnthropicChatClient`, D-031); its claim shape (`orgId` + `token.restrictions.allowedAssistantIds`/`allowedOrigins`) is a best-effort mapping to Vapi's documented restricted-public-JWT format, explicitly flagged as unverified against a live credential — confirm before any real call (tracked in TASKS.md). `modules/voice/token/issue-vapi-token.ts#issueVapiToken` is the orchestration: feature-flag check (`FEATURE_VOICE_EN`/`FEATURE_VOICE_UR` — **the first route in this codebase to actually read a `FEATURE_*` flag**, closing part of the gap found in Step 24), a dedicated rate limit tighter than the text concierge's (5/min vs. 10/min — minting a call credential is heavier than one chat turn), then token issuance; a thrown credential error resolves to a safe `INTERNAL` response with only an `errorType` logged, same "never leak detail" shape as `orchestrateTurn`'s catch block. `POST /api/vapi/token` reuses Step 20's `parseMutatingRequest` guard unchanged (session/CSRF/origin) and always embeds the server's own configured origin (`parseAppUrl()`), never a guest-supplied header value, as the token's origin restriction. Evidence: 24 new focused tests — JWT round-trip/tamper/expiry-boundary/malformed-token; per-locale assistant-id restriction correctness (English and Urdu tokens never carry each other's assistant id); the issued response never contains the private signing key; feature-flag gating (including that enabling one locale's flag does not unlock the other); per-session rate-limit enforcement and isolation; safe-fallback-on-throw with no leaked credential detail. 851 tests total (70 files); full `npm run verify` passes, including a production build listing `/api/vapi/token`. No live Vapi credential exists in this sandbox, so live JWT verification by the real Vapi API is unverified — the fail-closed path (missing `VAPI_PRIVATE_KEY`) is what's actually exercised.

- Runbook Step 32 authenticated Vapi tools and webhooks (D-036, Phase 6 underway): `POST /api/vapi/tools`/`POST /api/vapi/webhook` — server-to-server from Vapi, no session/CSRF, authenticated entirely by `modules/voice/webhook-auth.ts` reusing Step 12's `lib/security/webhook.ts` HMAC-SHA256/timestamp/replay verification completely unchanged (only new Vapi-specific header names and a fail-closed wrapper around `VAPI_WEBHOOK_HMAC_SECRET`, mirroring `cron-auth.ts`'s "unconfigured secret means reject everything" reasoning). Replay dedupe uses a SHA-256 hash of the raw signed body as the event id — robust regardless of which envelope fields Vapi actually sends. `modules/voice/tools/execute-vapi-tool-calls.ts#executeVapiToolCalls` dispatches through the exact same `modules/concierge/tool-registry.ts#dispatchToolCall` text chat's orchestrator calls (Step 27) — never a second tool implementation — serializing each result identically to `orchestrator.ts`'s own `tool_result` convention. `toolCallId` idempotency reuses Step 19's `runIdempotent`/`IdempotencyStore` unchanged, just a new scope; bounded execution caps calls-per-delivery at `MAX_TOOL_CALLS_PER_WEBHOOK` (5, overflow gets an honest per-call rejection, never silently dropped) and races each dispatch against `TOOL_CALL_TIMEOUT_MS` (8s) so one hung domain call can't block the whole webhook response. `modules/integrations/vapi-webhook.ts`'s tool-calls envelope schema is explicitly flagged as a best-effort mapping to Vapi's documented format (same caveat as D-035's JWT claims); `localeForAssistantId` derives locale authoritatively from which of *our own* configured assistant ids the call belongs to (never trusting client metadata for that), and `sessionIdForCall` uses a real `call.metadata.sessionId` when present or a call-scoped synthetic id otherwise — never a cross-guest collision either way. Evidence: 39 new focused tests across three files (valid/invalid-signature/tampered-payload/stale-timestamp/replayed/missing-header webhook auth; schema parsing including tolerant handling of unknown third-party fields and string-encoded arguments; real end-to-end dispatch through `getVenueInfo` proving the same-domain-service claim non-circularly; bounded-list overflow handling; toolCallId dedupe and fingerprint-mismatch conflict; a genuine fake-timer-driven timeout proof that a never-resolving dispatch still returns within `TOOL_CALL_TIMEOUT_MS`). 890 tests total (73 files); full `npm run verify` passes including a production build listing `/api/vapi/tools` and `/api/vapi/webhook`. Live check confirmed both routes fail closed (401) against unauthenticated requests and reject malformed bodies (400) with no live `VAPI_WEBHOOK_HMAC_SECRET` traffic possible in this sandbox.

- Runbook Step 33 voice web experience (D-037, Phase 6 underway): installed `@vapi-ai/web@2.7.0` (pinned exact) and read its real bundled types to *verify*, not guess, event names and payload shapes — correcting Step 32's `sessionIdForCall`/webhook schema along the way (the real path is `call.assistantOverrides.metadata.sessionId`, not `call.metadata`) since building this step required knowing the real client-side `assistantOverrides` field to embed a session id at call-start. `modules/integrations/vapi-web-client.ts` is a thin, one-file isolation of the real SDK import (ADR-0008), constructing `new Vapi(publicToken)` with Step 31's short-lived token. `app/[locale]/concierge/voice-call-state.ts` is a pure, fully-tested state machine (`voiceCallReducer`, `classifyVapiError`, `shouldShowPendingConfirmation`) — the same "pure/testable core, thin real-SDK-wiring adapter" split `orchestrator.ts`/`execute-vapi-tool-calls.ts` already use, since this project has never had React component tests and none were added here; `voice-panel.tsx` itself is verified live in the browser like every other page component since Step 13. `modules/concierge/prepare-tool-result.ts` (new) extracts `PendingConfirmation`/`PREPARE_TOOL_KIND`/`asPrepareToolResult` out of `orchestrator.ts` so voice and text share one definition. `modules/voice/pending-confirmation-store.ts` (new, in-memory, TTL) is the bridge for tool execution that happens entirely server-to-server (Step 32): `execute-vapi-tool-calls.ts` writes a successful prepare-tool result there, and the new `GET /api/vapi/pending-confirmation` (session-cookie-authenticated, no CSRF needed for a read) lets the browser poll for it. `pending-confirmation.tsx` (new) extracts the shared `PendingConfirmationCard`/`submitPendingConfirmation` out of `concierge-chat.tsx` (Step 28) so voice renders the byte-identical review card and hits the byte-identical submit endpoints — caught and fixed a real bug in passing: the draft `SourceChannel` type had an invented `'VOICE_CONCIERGE'` value that doesn't exist in `lib/schemas/common.ts`'s real enum (`VOICE_EN`/`VOICE_UR`). `/api/vapi/token`'s response now also echoes back `sessionId` (new `IssuedVapiTokenWithSession`) since the guest session cookie is `HttpOnly` and the browser has no other way to learn its own id to embed as call metadata. `concierge-mode-toggle.tsx` (new) is a labelled Type/Talk button group, the same accessible pattern `theme-toggle.tsx` (Step 14) uses; `page.tsx` only renders it when the requested locale's `FEATURE_VOICE_EN`/`FEATURE_VOICE_UR` flag is on, resolved server-side — a disabled flag removes the control entirely, not merely hides it client-side (Gate 1). Recording is never enabled — no code path calls `startRecording()`. Evidence: 41 new focused tests (the full call-state reducer including "an error status is never overwritten by a later call-end," real-SDK-confirmed error-string classification including Vapi's actual `customer-did-not-give-microphone-permission` `endedReason` value, and the duplicate-poll guard covering every combination of current/incoming/already-handled draft) plus the pending-confirmation store (TTL expiry, per-session isolation, latest-wins) and its wiring into tool execution (records only successful prepare-tool calls, never a failed one or an unrelated tool, latest replaces earlier). 931 tests total (75 files). Live check (English and Urdu) confirmed the Type/Talk toggle and full voice panel render and localize correctly, `aria-pressed` correctly reflects the active mode, "Start voice call" is correctly disabled while no CSRF token is available (matching this sandbox's standing `SESSION_SECRET`-unconfigured fail-closed path), RTL (`dir="rtl"`/`lang="ur"`) is correct, and no console errors beyond the expected `/api/session/csrf` 500s appeared. Full `npm run verify` passes cleanly, including a production build listing `/api/vapi/pending-confirmation` and no new guest-facing route beyond `/[locale]/concierge` itself (already existed) and confirming no Vapi credential/token material reaches the client bundle.

- Runbook Step 34 voice quality bake-off (D-038, Phase 6 done for now — 1 deferred): explicitly deferred, not executed — `cladium-research/operations/voice-bake-off-plan.md` (new) is the complete, ready-to-run test plan/protocol (prerequisites, a three-condition test matrix, a 15-scenario catalogue covering every item the runbook names plus two the localization spec independently requires, a scoring rubric with a non-negotiable safety veto, a consent/recording protocol, a session procedure, and a "turning results into code" section wiring back into `voiceStack`/`CHANGELOG.md`/`docs/vapi-deployment.md`), grounded throughout in real approved menu items/policy facts, never an invented placeholder. What cannot be done here: no session has been run, no score exists, no profile has been selected, no owner acceptance recorded — this needs real Pakistani English/Urdu speakers, their consent, and a live Vapi credential/deployed assistants, none of which exist in this sandbox (same standing limitation as D-034/D-035). Matches Step 18's exact precedent (D-022): build everything genuinely buildable, mark the step explicitly deferred rather than complete, don't count it toward the step-completion numerator. No code changed; `npm run verify`'s Step 33 result stands unchanged.

- Runbook Step 35 click-to-WhatsApp handoff (D-039, Phase 7 underway): `lib/business/whatsapp-link.ts#buildWhatsAppUrl(locale)` is the single hardened link builder for all four click-to-WhatsApp touch points (home, visit, menu-unpublished pages, and the concierge `getVenueInfo` CONTACT tool) — its `(locale: Locale) => string` signature structurally cannot carry guest data, satisfying `release-gates-v2.md` Gate 8's "avoids exposing customer data in a prefilled URL unless the guest explicitly chooses it" by construction, not convention. The `?text=` content is a new static, reviewed, bilingual `lib/i18n/chrome.ts` entry (`whatsappPrefilledMessage`) — generic courtesy text, not a business/policy claim needing owner-approved-Urdu gating. "Clear consent" is a visible external-navigation notice (`whatsappExternalNoticeText`) beside every WhatsApp link — UX clarity, not a stored consent record (Step 36's job). Bilingual staff-escalation copy extends to the concierge's own safe fallback: `orchestrator.ts`'s English-only `FALLBACK_REPLY`/`ESCALATION_REPLY` (Step 27) became locale-aware `fallbackReply`/`escalationReply` reading two new chrome.ts entries with chrome.ts's first interpolation token (`{whatsapp}`, reserved for the approved number) — an Urdu concierge session now gets an Urdu safe-fallback message. `WHATSAPP_URL`/`WHATSAPP_DISPLAY` themselves (Step 16) are unchanged. Evidence: 2 new test files plus extensions to `external-links.test.ts`, `concierge-get-venue-info.test.ts`, and `concierge-orchestrator.test.ts`; 946 tests total (78 files); full `npm run verify` passes; live browser check (English and Urdu) confirmed the correct prefilled URL, notice text, and untranslated brand name on all three pages.

- Runbook Step 36 consent and privacy controls (D-040, Phase 7 underway): `modules/consent/` — `lib/domain/consent-event.ts`'s `buildConsentEvent` is a pure builder written through a new `ConsentEventStore` (`consent-store.ts`) that behaves exactly like `AppendOnlySink` for every ordinary write, plus one deliberately separate `purgeExpiredBefore` capability reserved for the retention job alone, mirroring the real Postgres table's own new narrow exception. `consent-service.ts#getConsentSnapshot` folds the ledger into current per-category state, defaulting `ESSENTIAL_PREFERENCES` to granted (locale/theme, Steps 13–14, keep working with zero guest action) and `META_MARKETING`/`MICROPHONE`/`RECORDING` to not-granted (fail closed); a category's `policyVersion` mismatching the current `CONSENT_POLICY_VERSION` marks it `stale`, so a guest is asked again after a real policy change. `hasConsent` is the one primitive other modules must import to gate a real feature — wired immediately into `issue-vapi-token.ts`, which now rejects `POST /api/vapi/token` with a new `CONSENT_REQUIRED` (403) error when MICROPHONE consent isn't granted, checked server-side (not merely a hidden client button) before the feature-flag-gated feature-disabled check's sibling guard. New migration `20260829150001_consent_retention.sql` gives `consent_events` its one auditable DELETE exception (a SECURITY DEFINER function, transaction-local-flag-gated) since the Step 9 append-only trigger otherwise blocks even service_role unconditionally — `status_events`/`audit_events` are deliberately left untouched. `GET/POST /api/consent` (session-authenticated, CSRF-guarded on write) and `GET /api/cron/consent-retention` (CRON_SECRET-authenticated, mirrors `/api/cron/outbox-dispatch`) are the two new routes. `/privacy` (new) pairs an honest "not published yet, here's our WhatsApp" notice (never invented legal prose, same pattern as the unpublished menu, D-021) with the real, fully-working `ConsentPreferences` grant/revoke controls; `voice-panel.tsx` (Step 33) gained an inline "Allow microphone access" prompt shown instead of the Start Call button until MICROPHONE consent is granted. Evidence: 4 new test files (`consent-event`, `consent-store`, `consent-service`, `consent-retention`) plus extensions to `voice-token-issuance.test.ts` (the new `CONSENT_REQUIRED` rejection, and that a flag check short-circuits before the consent check ever runs), `errors-and-result.test.ts`, and `schemas.test.ts`; 974 tests total (84 files); full `npm run verify` passes including a production build listing `/[locale]/privacy`, `/api/consent`, and `/api/cron/consent-retention`, and the offline `check:db-config` migration check passing against the new migration (12 migrations, 28 tables, RLS unaffected — no new table). Live browser check (English and Urdu, via the same temporary-substitution-then-revert technique Step 17 established) confirmed the `/privacy` page's honest notice and footer link, the fail-closed "could not load your consent preferences" state with `SESSION_SECRET` unconfigured, and the microphone-consent prompt correctly replacing the Start Call button in both languages. No live Postgres exists in this sandbox (D-017), so the new migration's real DELETE-exception behavior is unverified beyond the offline structural checks — same standing caveat as every other migration since Step 8.

- Runbook Step 37 Meta measurement behind a flag (D-041, Phase 7 underway): `modules/integrations/meta-events.ts#trackMetaEvent` is the single flag-then-consent-gated decision point every trigger site calls through — `FEATURE_META_MARKETING` checked before `hasConsent('META_MARKETING')` (Step 36), matching `issue-vapi-token.ts`'s established order exactly, so "consent-denied sends nothing" is enforced once, not re-derived per call site. `lib/schemas/common.ts#metaEventNameSchema` is a closed, custom seven-name catalog matching `production-architecture-v2.md` §11 verbatim — never Meta's own `Purchase`/`Schedule` standard events, making "request events never emit purchase/booking confirmation" a type-level guarantee, not a runtime check that could be forgotten. `modules/integrations/meta-client.ts#createMetaEventClient` (real CAPI adapter, ADR-0008, never live-called — no `META_*` credentials in this sandbox) races every send against a 3s timeout so a slow/unreachable Meta endpoint can never block the guest-facing request that triggered it, the same reasoning `execute-vapi-tool-calls.ts`'s `TOOL_CALL_TIMEOUT_MS` established (Step 32); a failed/slow send still returns the generated `eventId` so the independent browser Pixel channel can report safely with no double-count risk. Wired into real trigger points for five of the seven events: `add_to_cart` (`POST /api/takeaway/cart/items`), the three `submit_*_request` events (their existing `submit` routes, firing only on success), and `view_menu` (new client `MenuViewTracker`, mounted in both branches of `menu/page.tsx`). `contact`/`lead` are fully built and tested but deliberately not yet click-wired into the three existing WhatsApp links (Step 35) — a separable refactor tracked in TASKS.md rather than rushed in. The actual browser Meta Pixel `<script>` bootstrap and the CSP `script-src`/`connect-src` relaxation it would need are also deliberately left unbuilt this step — `lib/analytics/meta-track-client.ts` already forwards the shared `eventId` to `window.fbq(...)` the moment that script exists, a safe no-op today. Evidence: 3 new test files (`meta-events`, `meta-client`, `meta-schemas`) plus extensions to `env.test.ts` and `schemas.test.ts`; 1000 tests total (86 files); full `npm run verify` passes including a production build listing `/api/meta/track` and a client-bundle scan confirming no Meta credential material reaches the browser (no migration change this step — `check:db-config` still reports 12 migrations/28 tables). Live browser check (English and Urdu) confirmed `/menu` and `/book` render unaffected and `MenuViewTracker`'s CSRF bootstrap fetch fires and fails closed exactly like every other guest-triggered fetch in this sandbox.

- Runbook Step 38 WhatsApp Cloud readiness (D-042, **Phase 7 complete**): `cladium-research/operations/whatsapp-cloud-readiness.md` (new) is the readiness report — a prerequisites checklist matching `release-gates-v2.md` Gate 8 verbatim, what is/isn't built, and a "turning this into a live integration" section, explicitly distinguishing Cloud API readiness from click-to-WhatsApp (Step 35), which needs none of it. `modules/integrations/whatsapp-client.ts#createWhatsAppCloudClient` is a real adapter matching Meta's documented template-message contract, never called by anything — a genuine "disabled adapter scaffold," not a wired feature (unlike Step 37's Meta events, which had six named trigger points). `modules/integrations/whatsapp-webhook-auth.ts` implements Meta's real two-secret webhook contract — `verifyWhatsAppWebhookSignature` (`X-Hub-Signature-256`, plain HMAC-SHA256 over the raw body, no timestamp — deliberately its own function, not a forced reuse of Vapi's differently-shaped verifier) and `verifyWhatsAppWebhookChallenge` (the separate `hub.verify_token` handshake). `GET/POST /api/whatsapp/webhook` checks `FEATURE_WHATSAPP_CLOUD` first, before anything else — off means `404`, this step's own evidence bullet. The live check caught a real bug before commit: calling `isFeatureEnabled` directly 500'd in this sandbox (every flag unset) instead of 404ing, the same pre-existing all-flags-required gap already tracked since Step 36 — fixed with a small, local `isWhatsAppCloudEnabled()` try/catch wrapper scoped to this one route, not a change to the shared `isFeatureEnabled`/`parseFeatureFlags` functions. Evidence: 3 new test files (`whatsapp-client`, `whatsapp-webhook-auth`) plus extensions to `env.test.ts`; 1024 tests total (88 files); full `npm run verify` passes including a rebuild listing `/api/whatsapp/webhook` and a client-bundle scan confirming no WhatsApp credential material reaches the browser (no migration this step). Live browser check re-verified after the fix: both `GET`/`POST` correctly return `404 FEATURE_DISABLED`.

- Runbook Step 39 cross-product E2E and accessibility (D-043, Phase 8 underway): installed Playwright (`@playwright/test`) and `@axe-core/playwright`; `playwright.config.ts` runs `tests/e2e/` against a real `next dev` server (`workers: 1` — documented, reproduced concurrency flakes justify it) across three device projects (mobile/tablet/desktop, all pinned to `browserName: 'chromium'` after WebKit's absence broke the two device-preset projects) fed a synthetic, non-secret, local-only `webServer.env` (`SESSION_SECRET`/`CRON_SECRET`/feature flags/`STAFF_DEV_ACCOUNTS`) — a genuine unlock for real session/CSRF/staff-auth/domain-service E2E coverage this sandbox's standing "cannot set real third-party secrets" limit had blocked everywhere else, while deliberately still leaving `ANTHROPIC_API_KEY`/`VAPI_*`/`META_*`/`WHATSAPP_*` unset to prove the app's own safe-fallback paths for real, not merely in unit tests. Eleven new spec files exercise every dimension the runbook step names by name (locale/metadata, the full mobile×tablet×desktop×locale×theme matrix, keyboard/landmarks, reduced motion, 200%-zoom simulation, WhatsApp handoff, booking flow, event flow, concierge chat/voice shell, staff roles, error states). The suite surfaced and fixed four real application bugs: `isFeatureEnabled` (`lib/env.server.ts`) throwing for *any* unrelated missing flag instead of degrading only the queried one to `false` — the same all-flags-required gap Step 38 had already local-patched around in one route; fixed at the source and the Step 38 workaround removed. `/staff` had no `<main>` landmark (axe `landmark-one-main`), fixed in `src/app/staff/layout.tsx`. Two nav links failed WCAG 2.2 AA's 2.5.8 target-size minimum, fixed with `nav a { padding }` in `globals.css` (a first attempt using an offsetting negative margin caused a *new* overlap-driven axe violation, caught by re-running the same diagnostic and reverted in favor of the simpler, correct fix). A real race condition in `voice-panel.tsx` — two independent mount-time fetches (`/api/session/csrf` and `/api/consent`) could resolve out of order and leave a stale CSRF token — fixed by merging them into one sequenced effect. Two findings were investigated and documented rather than fixed: `notFound()` under a Suspense-containing catch-all route returns HTTP 200, confirmed via live `WebSearch` as a well-documented upstream Next.js App Router limitation (not app-specific) needing a `proxy.ts`-level fix judged too large for this step; and a per-page canonical/hreflang gap. `.github/workflows/ci.yml` gained a new `e2e` job (Chromium install, `npx playwright test`, report artifact upload). Evidence: 240/240 E2E tests passing across all three device projects in the final clean run; unit suite still 1027/1027 (85 files); full `npm run verify` (typecheck/lint/format/unit/validators/scans/build) clean; `scan:build-output`/`scan:secrets`/`check:db-config` all clean. No migration this step (28 tables, unchanged).

- Runbook Step 40 security and abuse verification (D-044, Phase 8 underway): `cladium-research/operations/security-abuse-verification-report.md` (new) is the full evidence matrix. Found and fixed three real gaps. (1) Only 2 of 34 routes had any rate limit — `lib/http/route-rate-limits.ts` (new shared, namespaced, in-memory limiter) wired via `mutating-route.ts#parseMutatingRequest`'s new optional `rateLimit` option into all 10 guest-mutation routes (cart edits, review/submit for all three request types, consent, Meta tracking) plus the staff sign-in route (keyed by attempted staffId, not client IP — no live Vercel deployment exists here to confirm `x-forwarded-for`'s real shape). Verified via a new `tests/e2e/rate-limiting.spec.ts` hitting the real dev server. (2) `lib/security/headers.ts`'s CSP/HSTS/X-Frame-Options/Permissions-Policy/Referrer-Policy — fully built and unit-tested since Step 12 — were never actually wired into any response; `next.config.ts` had no `headers()` at all. Fixed by wiring `securityHeaders()` into every non-dev response (confirmed via live `WebSearch` against Next.js's own current CSP-nonce docs before implementing; skipped in `next dev` after live-checking found Turbopack's own dev tooling breaks under `X-Content-Type-Options: nosniff` — a dev-only quirk, not an app defect); verified against a real `next build && next start` server: headers present exactly as built, full render/hydration, theme toggle click-tested, no console errors. `'unsafe-inline'` remains in `script-src`/`style-src` pending a separately-scoped nonce migration (needs converting 3 static routes to dynamic rendering). (3) Re-verifying with a fresh full E2E run surfaced a real, reproduced cross-component session race in `/concierge`, unrelated to fixes 1–2: `ConciergeChat` (default Type mode) fires its own mount-time `GET /api/session/csrf`; tapping "Talk" immediately unmounts it before that fetch resolves (the request itself isn't aborted) while sibling `VoicePanel` mounts and fires its own `GET /api/consent` — under full-suite load the stale response occasionally lands last, silently overwriting the session cookie `VoicePanel` had already captured a CSRF token for, causing its consent-grant call to fail `403`. The exact class Step 39 fixed *within* `VoicePanel` (D-043), now found *across* it and its sibling — root-caused from the failing test's own network trace, not assumed from a timeout bump alone (a timeout raise tried first did not fully resolve it). Fixed by lifting the one session/consent-bootstrap fetch to the shared parent `ConciergeModeToggle`, passed down as props to both children (`ConciergeChat`'s other standalone caller, `page.tsx` with voice unavailable, still self-fetches — no sibling to race with there). Also verified clean, no remediation needed: RLS (offline `check:db-config`, 28/28 tables), XSS/injection (structural, zero raw-HTML/eval surfaces), SSRF (structural, zero guest-influenced outbound URLs), request-smuggling assumptions (already-correct `Content-Length`/origin handling since Step 12), webhook spoof/replay (Steps 32/38, unchanged), prompt injection (Steps 27/29, unchanged), `npm audit` (0 vulnerabilities/500 deps), secret scan, client-bundle scan. Also fixed in passing: `eslint.config.mjs` gained `playwright-report/**`/`test-results/**` ignores (found live: lint failed 186 errors against Playwright's own minified trace bundle). Evidence: 10 new/extended tests; 1037 total (86 files); full `npm run verify` clean; `concierge-shell.spec.ts` passes cleanly 15/15 across all three device projects after the fix; final full 249-test E2E re-run scored 248/249 with zero `concierge-shell.spec.ts` failures — the sole remaining failure was an unrelated `matrix.spec.ts` `/ur/privacy` a11y check hitting Playwright's own 30s context-teardown timeout (not an assertion failure), reproduced clean in isolation immediately after and consistent with resource contention from four consecutive full-suite runs this step, not a defect.

- Runbook Step 41 performance and resilience (D-045, Phase 8 underway): `cladium-research/operations/performance-resilience-report.md` (new). `tests/unit/performance-load.test.ts` (new, 7 tests) pushed every deterministic in-memory service to hundreds of concurrent operations (300 concurrent guest submissions, a 500-event outbox backlog across 26 dispatch cycles, worker-overlap races, 50-way optimistic-lock contention, 150 concurrent concierge sessions, 500 real HMAC-signed Vapi JWTs) — all clean, real numbers recorded. Found and fixed a real bug doing exactly this: `lib/security/rate-limit.ts`'s in-memory adapter returned the same mutable window object on every call, so a genuine `Promise.all` burst let every caller read the *final* mutated count — 200 concurrent calls against a 20/min rule let zero requests through instead of the correct 20 (the opposite of intended fail-open behavior). Fixed by returning a fresh value snapshot each call; this is the same limiter backing Step 40's guest-route/staff-signin/concierge/Vapi-token rate limits, which Step 40's own sequential-load E2E test never exercised at true-burst volume. Provider timeout budgets (20s concierge turn, 8s concurrent Vapi tool calls, 3s Meta events) were cross-checked against Vercel Pro's real documented function-duration limits (confirmed live via `WebFetch`: 300s default) — 15×–100× headroom, no route overrides `maxDuration`. Core Web Vitals: a genuine local proxy measurement (FCP/LCP ~164ms, CLS 0, four pages) captured via a real Playwright Chromium instance, after finding this session's own embedded browser-pane tool never fires Paint Timing/LCP under its rendering path (`visibilityState: "hidden"`) — the real Gate 7 P75 measurement stays correctly scoped to Step 43's staging telemetry. Realtime interruption: not applicable, feature deliberately unbuilt (ADR-0007/D-017). Proposed alert thresholds recorded for Step 43/46. Evidence: 1043 unit tests total (87 files, up from 1037); full `npm run verify` clean; a fresh full E2E re-run scored a completely clean 249/249 across mobile/tablet/desktop.

- Runbook Step 42 backup, restore, and rollback drill (D-046, **Phase 8 complete**): `cladium-research/operations/backup-restore-rollback-drill-report.md` (new). Docker was reachable in this sandbox for the first time since Step 7/10 — ran a genuine local Postgres drill rather than deferring. Found and fixed a real, previously-undiscovered gap: a fresh `db reset` (no restore involved, a pure control test) failed `db:test:rls` outright — every table, `confirmation_tokens`/`idempotency_keys` included, carried full `anon`/`authenticated` CRUD grants via a Supabase platform default `grants.sql` (Step 10) never revoked, undetected through 31 subsequent steps since `db:test:rls` needs a live database this sandbox didn't have until now. Fixed with a new migration (`20260830044140_fix_default_table_privileges.sql`) matching Supabase's own documented remediation (confirmed via live `WebSearch`): `ALTER DEFAULT PRIVILEGES` for all future tables plus `REVOKE ALL`+re-`GRANT` (verbatim from `grants.sql`) for the existing 28 — verified clean via `db:test:rls`/`db:test:schema` post-fix. The backup/restore drill itself took 5 iterations to reach a clean, production-representative method (schema-scope the dump, never strip privileges, don't pre-create the target schema, tolerate one confirmed-benign local-role-hierarchy quirk) — each iteration a real, kept finding. Final clean timed run: backup 852ms, full restore 10.1s (all 30 relations), critical test records intact, both live test suites PASS post-restore. Application rollback: confirmed via live `WebSearch` that Vercel's instant rollback is a routing switch that never touches the database — exactly why this project's zero-destructive-DDL migration discipline (verified across all 13 migrations) is what makes that rollback safe. Proposed RTO 30min/RPO 5min recorded pending owner approval; named operators explicitly left for the owner to assign, not invented. Evidence: 1044 unit tests total (87 files, unaffected); full `npm run verify` clean; local Supabase stack stopped after the drill. **Phase 8 ("full hardening") is now complete — 4/4, Steps 39–42.**

- Runbook Step 43 staging release (D-047, Phase 9 underway): `cladium-research/operations/staging-release-report.md` (new). The first deployment to real, externally-hosted infrastructure — a real Vercel project (Hobby plan, owner's choice; Pro upgrade tracked for before production) and a real, isolated Supabase project (`vxvpxywszskxcugwpsch`), both owner-created. All 13 migrations applied via `supabase db push`; `db:test:schema`/`db:test:rls` both re-run live against real hosted Postgres and passed. A third-party AI-gateway relay was investigated and rejected for the Anthropic key (no code `baseURL` override exists; the relay's keys returned `401`; its install method was a blind `irm | iex`, independently blocked by Claude Code's own safety classifier) — a real Anthropic key was obtained instead. Found and fixed a real deployment gotcha: Vercel's cache-reusing "Redeploy" didn't pick up a changed `NEXT_PUBLIC_APP_URL`, causing every mutating request to fail closed with `403` until a genuinely fresh rebuild was triggered — root-caused via direct testing (correct Origin header + correct CSRF token still rejected), not guessed. Full live smoke-test pass: real booking and event requests submitted end to end; double-click idempotency proven against real Postgres (identical `requestId` both times); staff sign-in and both queues correct; outbox dispatcher manually triggered, 3/3 delivered with PII-free payloads; WhatsApp link and consent grant/revoke both correct; every disabled-flag route fails safely; and a real Anthropic API call returned a correct, accurate, on-brand reply — the first genuine end-to-end proof of the concierge integration in this entire project. No application source code changed this step. Evidence: `db:test:schema`/`db:test:rls` pass live; local `npm run verify` unaffected from Step 42.

- Runbook Step 44 owner and staff UAT (D-048, **Phase 9 underway**): `cladium-research/operations/owner-staff-uat-report.md` (new). A live collaborative verification pass against the real staging deployment/database — Claude drove a real browser and a Supabase MCP connection under the owner's real-time direction, rather than the owner/staff independently testing on their own devices; recorded honestly as owner-approved functional verification, not staff self-certification, with a genuine independent staff walkthrough left as an open follow-up. Bilingual/RTL, themes, honest not-published menu messaging, request wording, WhatsApp, and privacy/consent all passed live verification (real contrast-ratio measurements, real consent grant/revoke round-trips). Operations: a real (clearly fake) booking and event request submitted end to end, both landed in staff queues, one walked through a real `REQUESTED → CONFIRMED` staff transition with correct audit history. Concierge: the real Anthropic integration answered correctly and safely declined to guess on a severe-allergy question, in English and Urdu. Found and closed a real gap mid-step: no scoped-role test account existed in staging, so role-scoping was untestable — added `order_staff` (`ORDER_STAFF`) to `STAFF_DEV_ACCOUNTS` with the owner's go-ahead, confirmed a normal Vercel redeploy (no cache-free rebuild) picks up a server-only env var change, then verified a clean `403` (never `500`) on out-of-scope queues. Voice stays deferred (D-038, unchanged). Owner gave verbal sign-off. No application source code changed this step — one Vercel env-var update plus a new report.

- Runbook Step 45 production readiness decision (D-049, Phase 9 underway): `cladium-research/operations/production-readiness-decision.md` (new). Evaluated all 10 gates in `release-gates-v2.md` against the full evidence record since Step 1. **Decision: NO-GO** — expected, not a surprise: Gate 0 (owner content/business decisions — all 10 items still open), Gate 3 (real staff auth/MFA genuinely unbuilt — the one hard technical blocker, distinct from every flag-deferred capability), and Gate 9 (Vercel Pro/custom domain/named contacts) all have real open items. Gate 6 (Vapi voice) stands as the model of a clean, correctly-handled deferral by contrast — `FEATURE_VOICE_EN`/`FEATURE_VOICE_UR` false everywhere, no assistant/credential exists. Three genuinely new findings this step, none previously checked: (1) the Supabase project is region `ap-northeast-1` (Tokyo) and Vercel's function region has never been checked/configured against it — a real, previously-unconsidered latency risk; (2) `npm audit` has no automated CI step at all — Step 40's "0 vulnerabilities" was a one-time manual check, never repeated; (3) the three unenforced request-type feature flags (known since Step 24) were re-examined against Gate 1's literal "reject direct calls to disabled routes" wording and confirmed to be a real gate failure, not just a nice-to-have. The report closes with two explicitly separated punch lists — owner/business items this build process cannot supply, and engineering items it can do once scheduled. No application source code changed this step; `npm run verify` unaffected from Step 44.

- Real staff authentication with structurally-enforced owner/manager MFA (D-050, Phase 9 follow-up work, not a numbered runbook step): closes Step 45's one hard technical blocker. `modules/integrations/supabase-auth-client.ts`/`supabase-admin-client.ts` (provider-neutral, ADR-0008, every API shape verified against `@supabase/supabase-js`'s real shipped `.d.ts` files before writing code, the same discipline Step 33 applied to `@vapi-ai/web`), `modules/staff/supabase-directory.ts` (real `StaffDirectory` keyed by `staff_profiles.id`, fail-closed to `null` on any error including full misconfiguration — needed so `deps.ts`'s new `createCompositeStaffDirectory` behaves as "no real accounts yet" rather than crashing in every environment without real Supabase configured), `modules/staff/supabase-credentials.ts` (the orchestration — an OWNER/MANAGER account can never reach `SIGNED_IN` without a verified TOTP factor, unconditional on role, never on what Supabase's own `nextLevel` calculation happens to say), `modules/staff/mfa-session.ts` (a short-lived signed `HttpOnly` cookie bridging sign-in/enrollment's two legs, reusing Step 31's real JWT signer rather than duplicating it), two new routes (`POST /api/staff/mfa/enroll`, `POST /api/staff/mfa/enroll/verify`, both rate-limited), and a toggled real sign-in UI in `app/staff/dashboard.tsx` — never mounted alongside the existing dev form, so `tests/e2e/staff-roles.spec.ts` needed no changes. Additive, not a cutover: dev and real accounts coexist safely (no id-space collision between short dev ids and real UUIDs), so staging's existing `STAFF_DEV_ACCOUNTS` sign-ins kept working unchanged through this deploy. `cladium-research/operations/real-staff-account-setup.md` (new) is the owner-facing provisioning procedure — creating the actual account/password/QR-code enrollment is deliberately left to the owner, matching this project's standing rule against handling real credentials on their behalf. Evidence: 1087 unit tests total (91 files, up from 1044); full `npm run verify` passes including a production build listing both new routes and a client-bundle scan confirming the service-role key never reaches the browser.

- Wired the three unenforced request-type feature flags into their guest-facing routes (D-051, Phase 9 follow-up work, closes a real Gate 1 failure from Step 45/D-049): `parseMutatingRequest` gained an optional `featureFlag` parameter, checked first, before content-type or session resolution — matching `errors.ts#featureDisabled`'s "a disabled feature must not confirm it exists" reasoning. All 8 mutating routes across takeaway/bookings/events plus `GET /api/takeaway/cart` now fail closed to `404` when their entity's flag is off; the concierge's own read tools are deliberately unaffected (they never go through these HTTP routes). `tests/unit/mutating-route.test.ts` (new). 1092 unit tests total (92 files); full `npm run verify` passes clean. No live staging toggle test — staging already runs all three flags `true`, so this deploy is a no-op there by design.

## Next

1. Review this state and `.continuum/TASKS.md`.
2. **Phase 9 ("staging, UAT, and production") is underway — Steps 43–45 complete, 3/5. Step 45's decision was NO-GO (D-049).** Runbook Step 46 (controlled production deployment) explicitly requires an approved GO first, per its own instruction — it cannot start yet. Active work is resolving `production-readiness-decision.md`'s punch list (owner/business items the build process cannot supply, and engineering items it can do once scheduled — see items 32–36 below), then re-running Step 45's evaluation for a fresh go/no-go. The one hard technical blocker (item 32) is now built (D-050) — what remains there is the owner provisioning real accounts.
3. Note for later: full-stack local work (Studio/Storage/Realtime/Edge Functions) needs Docker raised to ~7 GB; the migration workflow itself does not.
4. Note for later: revisit Step 18 (menu carousel) once the menu is published and photos are approved.
5. Note for later: a real Postgres/Supabase adapter for `lib/domain/`'s repository interfaces is still needed before Steps 19–25 can persist for real (D-023) — build it once a live database connection is available (see D-017). It must implement `findOrBegin`/`claimIfUnused`/`claimBatch` as single conditional writes (D-025/D-029), not a read-then-write.
6. Note for later: build the takeaway cart/review UI pages once the menu is actually published (D-024) — the API is ready and waiting.
7. Note for later: real staff authentication (Supabase Auth + owner/manager MFA linked to `staff_profiles`) is needed before Step 24's workspace is production-ready (D-028) — replaces `modules/staff/dev-credentials.ts` without any route changing, once a live Supabase project exists (D-017).
8. **Resolved (D-051)**: `FEATURE_TAKEAWAY_REQUESTS`/`FEATURE_BOOKING_REQUESTS`/`FEATURE_EVENT_REQUESTS` are now wired into every guest-facing route for each entity (`lib/http/mutating-route.ts#parseMutatingRequest`'s new `featureFlag` option, plus `GET /api/takeaway/cart` inlined the same check).
9. Note for later: a real Vercel Cron schedule for `/api/cron/outbox-dispatch` is a deployment-step (Step 46) concern, not built in Step 25 — the endpoint exists and is authenticated, but nothing invokes it yet outside tests.
10. Note for later: local dev secrets (`SESSION_SECRET`, `NEXT_PUBLIC_APP_URL`, `CRON_SECRET`, `ANTHROPIC_API_KEY`, and now `VAPI_*`) still cannot be set in this sandbox (writes to any `.env*` file are denied by permission settings) — a live end-to-end check of the happy-path cart/CSRF/booking/event/staff/dispatch/concierge/voice flow (as opposed to the fail-closed path, already verified live) needs an environment where that restriction doesn't apply, or the user setting them directly.
11. Note for later: `release-gates-v2.md` Gate 5's "critical policy evals pass 100% in English, Urdu script, and Roman Urdu" needs every `requiresLiveModel: true` case in `modules/evals/cases/` actually run (not skipped) and passing — blocked on the same `ANTHROPIC_API_KEY` limitation as items above, tracked in TASKS.md until a live key is available.
12. Note for later: no real Vapi assistant has been created in any environment yet (Step 30 built only the content templates) — `docs/vapi-deployment.md`'s procedure needs a human with Vapi org access to actually run it, and (Step 31 built the token service) can now be reached from the browser once one exists.
13. Note for later: `modules/integrations/vapi-webhook.ts`'s tool-calls envelope shape and `modules/integrations/vapi-web-client.ts`'s event names are now confirmed against `@vapi-ai/web@2.7.0`'s real bundled types (Step 33) — only two things remain genuinely unverified against a live credential: `modules/integrations/vapi-client.ts`'s restricted-JWT claim shape (`orgId`/`token.restrictions.allowedAssistantIds`/`allowedOrigins` — this is a *server-signed* token format, not something the client SDK's types describe) and `modules/voice/webhook-auth.ts`'s `x-vapi-timestamp`/`x-vapi-signature` header names (this codebase's own choice for the Custom Credential integration, not dictated by the SDK either). Confirm both against Vapi's current API docs/dashboard Custom Credential configuration before any of this code handles a real call (D-035/D-036).
14. Note for later: Step 34's voice quality bake-off (D-038) is fully planned (`cladium-research/operations/voice-bake-off-plan.md`) but not executed — needs a live Vapi credential/deployed assistants (item 12) plus recruited, consented real Pakistani English/Urdu speakers. Run it before `release-gates-v2.md` Gate 6 can be considered satisfied; the plan's own "turning results into code" section explains exactly how the resulting evidence updates `modules/voice/profiles/templates.ts`'s `voiceStack`.
15. Note for later (found Step 39): `notFound()` thrown under a Suspense-containing catch-all route (`app/[locale]/[...rest]/page.tsx`) returns HTTP 200, not 404 — a documented upstream Next.js App Router limitation (confirmed via live `WebSearch`, cited GitHub discussions #70170/#76501 and issue #76474), not app-specific. `tests/e2e/errors.spec.ts` documents the exact current behavior. A real fix needs a `proxy.ts`-level check per the Next.js team's own guidance — judged too large/risky to fold into Step 39; revisit as a scoped follow-up.
16. Note for later (found Step 39): per-page canonical/hreflang metadata beyond Step 13's locale-root handling has a gap — `tests/e2e/locale-metadata.spec.ts` documents current behavior. Revisit as a scoped follow-up, likely alongside Step 45's production-readiness review.
17. Note for later (found Step 40): `securityHeaders()`'s CSP `connect-src` is `'self'` only — before `FEATURE_VOICE_EN`/`FEATURE_VOICE_UR` are ever enabled in a real environment, confirm Vapi's actual required browser-SDK origins (`@vapi-ai/web` connects out directly from the browser) against Vapi's current docs and add them via `securityHeaders({ connectSrc: [...] })`, or voice calls will fail silently under CSP.
18. Note for later (found Step 40): CSP is applied statically (no per-request nonce), so `script-src`/`style-src` include `'unsafe-inline'` — the honest baseline Next.js's own inline hydration scripts need without one. A real nonce needs every page to opt into dynamic rendering (Next's own documented requirement); this app still has three statically-rendered routes (`/`, `/_not-found`, `/staff`). Converting those to drop `'unsafe-inline'` is a genuine, separately-scoped architectural decision (loses static generation/ISR site-wide) — do not fold it into an unrelated future step.
19. Note for later (found Step 40): `scripts/db/rls-tests.sql`'s live allow/deny matrix was not re-run this step — no Docker/Supabase stack is running in this sandbox. The offline `check:db-config` structural check (28/28 tables RLS-enabled) was re-confirmed instead. Re-run the live matrix once a database is available (same standing item as #5 above, D-017).
20. Note for later (found Step 41): Core Web Vitals could only be measured as a local `localhost` proxy (FCP/LCP ~164ms, CLS 0) — Gate 7 itself scopes the real P75 measurement to staging/production telemetry. Re-measure for real once Step 43's staging environment exists, and again once real hero photography ships (today's identical FCP/LCP reflects the current text-only content, not a representative future measurement).
21. Note for later (found Step 41): `performance-resilience-report.md`'s proposed alert thresholds (outbox failure rate, guest-route 429 rate, provider-timeout rate, concierge turn-deadline-hit rate) have no monitoring/alerting stack to wire into yet — configure them for real once Step 43/46's observability stack exists.
22. Note for later (found Step 41): the real production `RateLimitStoreAdapter` (Redis/Supabase-backed, D-023) still needs building — whoever builds it should re-read D-045's reasoning about the in-memory adapter's live-mutable-reference bug first, since the same mistake shape could reappear there.
23. Note for later (found Step 41): load tests only exercise in-memory reference stores (D-023) — real Postgres/Supabase contention (connection-pool exhaustion, real lock wait times, real query planning under load) remains unmeasured pending a live database adapter and a live Supabase project (D-017).
24. **Capability update (Step 42)**: local Docker + the Supabase CLI's reduced stack IS reachable in this sandbox — confirmed live. Does not resolve D-017 (no hosted Supabase project/staging/production/real PITR) but means `db:test:schema`/`db:test:rls`/a real `db reset` can run live going forward if Docker Desktop is started first. Re-check this before assuming a DB task is blocked purely on "no live Postgres."
25. Note for later (found Step 42): the next migration author should re-read `20260830044140_fix_default_table_privileges.sql`'s own doc comment before adding any new table — Supabase's default-grant behavior applies to every new table unless the `ALTER DEFAULT PRIVILEGES` that migration set up continues to hold; verify it survives any future `db reset`, not just this step's.
26. Note for later (found Step 42): the backup/restore drill used generic `pg_dump`/`psql` against local Postgres, not Supabase Pro's actual managed PITR/backup system (WAL-based, dashboard/API-driven — operationally different, likely faster/more reliable). Re-run a real timed drill against an actual Supabase Pro project once one exists (Step 43/45); do not treat this step's ~11s local RTO as a production estimate.
27. Note for later (found Step 42, still open after Step 44): proposed RTO (30 min) / RPO (5 min) and Gate 9's named launch owner/incident contact/rollback decision-maker still need real owner review and real names — not collected during Step 44's UAT pass. Confirm/adjust at Step 45 (production readiness decision) at the latest.
27a. Note for later (found Step 44): the notification response procedure (who checks for new requests, how often, what happens if nobody responds in time) also remains undecided — an owner staffing decision, needed before Step 45's go/no-go alongside item 27.
28. Note for later (found Step 43): Vercel's cache-reusing "Redeploy" can silently leave a changed environment variable inactive — any future deploy (staging or the eventual production one, Step 46) that changes request-time-relevant env vars needs an explicit fresh rebuild, not just Redeploy. Worth carrying into a real deployment runbook, not just this step's report.
29. Note for later (found Step 43, re-confirmed cleared before Step 44, then reintroduced by Step 44 itself): Step 43's original test data was confirmed already gone before Step 44 began; Step 44 then added its own real, staging-only test data (one booking, one event request, clearly marked "TEST — UAT Step 44") plus a test transition audit entry — same harmless, not-blocking pattern. Worth clearing before a genuine independent staff walkthrough or before Step 46, whichever comes first.
29a. Note for later (found Step 44): a third `STAFF_DEV_ACCOUNTS` entry (`order_staff`, `ORDER_STAFF`) now exists in staging alongside owner/manager, added to make role-scoping testable. Same standing requirement as before (D-028): all three are dev-only scaffolding that must never exist in any production env var, superseded once real Supabase Auth (Gate 3) ships.
29b. Note for later (found Step 44): this step's UAT was a live collaborative session (Claude driving the real site/database under the owner's direction and sign-off), not independent owner/staff testing on their own devices. Schedule a genuine walkthrough with each real staff role on their own phone before Step 46.
30. Note for later (found Step 43): Vercel Hobby was used deliberately for staging — confirm the upgrade to Pro happens before Step 46 (controlled production deployment), per Gate 9 and `deployment-target.md`.
31. Note for later (found Step 43): a real Anthropic key now exists in at least one environment (staging) — a good moment to actually run the `requiresLiveModel: true` eval cases (Step 29, D-033) against it rather than leaving them permanently skipped, ahead of Gate 5's full requirement.
32. **Resolved (D-050)**: the one hard technical blocker to a GO decision — real Supabase Auth + owner/manager MFA — is now built and tested (`modules/staff/supabase-*`, `modules/integrations/supabase-*-client.ts`). What remains is entirely the owner's: provisioning real accounts (`cladium-research/operations/real-staff-account-setup.md`) and, once confirmed working, removing `STAFF_DEV_ACCOUNTS` from staging.
33. **Resolved (D-051)**: item 8's flags are now wired and tested (`tests/unit/mutating-route.test.ts`).
34. Note for later (found Step 45, D-049 — genuinely new finding): the Supabase project is region `ap-northeast-1` (Tokyo); Vercel's function region has never been checked or explicitly configured against it. Verify alignment (set explicitly, e.g. `hnd1`, if needed) before the Pro upgrade/production deploy.
35. Note for later (found Step 45, D-049 — genuinely new finding): `npm audit` has no automated CI step — Step 40's "0 vulnerabilities" was a one-time manual check, never repeated or automated since. Add it to `verify`/CI before launch.
36. Note for later (found Step 45, D-049): re-run the backup/restore drill (Step 42, item 26 above) and the load/concurrency tests (Step 41, item 23 above) against real Supabase Pro once upgraded — both remain proven only at the local-Postgres/in-memory layer.
37. Note for later (D-050): real staff *accounts* still need provisioning in staging — the auth code path is built and tested, but no real Supabase Auth user/`staff_profiles` row/verified MFA factor exists yet. Owner action, procedure in `cladium-research/operations/real-staff-account-setup.md`. Remove `STAFF_DEV_ACCOUNTS` from staging once confirmed working.

## Production blockers

Owner-approved menu/Urdu publication signoff (menu content itself is now structurally verified complete — 118 items, 12 categories, no empty categories); vector/transparent logo and 20–40 licensed photos; primary phone and staff roles/workflows; tax/service charges and collection-payment rules; booking/event rules; privacy/retention/consent wording; real-speaker bilingual Vapi bake-off; business-owned Meta/WhatsApp configuration for later flagged integrations.

## Load next, not everything

Read root `CLAUDE.md`, `.continuum/TASKS.md`, and `cladium-research/architecture/context-routing-v2.md`. Load only the task-specific sources it names. Never paste the full menu, raw command output, or whole runbook into context unless the active step requires it.
