# Cladium task ledger

## Active

- [ ] P1 — Step 32: authenticated Vapi tools and webhooks. Implement `POST /api/vapi/tools`/`/api/vapi/webhook` with Custom Credential HMAC-SHA256 verification (reuse `lib/security/webhook.ts` from Step 12), timestamp freshness, constant-time comparison, replay rejection, `toolCallId` idempotency, strict payload schemas, bounded execution, and redacted logging — invoking the same domain services text chat already uses (Step 26's tools), never a second implementation. No live `VAPI_WEBHOOK_HMAC_SECRET` traffic exists in this sandbox; route/validation/fail-closed logic is buildable and testable now.

## Next

- [ ] P2 — `modules/integrations/vapi-client.ts`'s restricted-JWT claim shape (`orgId`/`token.restrictions.allowedAssistantIds`/`allowedOrigins`) is a best-effort mapping to Vapi's documented format, never verified against a live credential (Step 31, D-035) — confirm it against Vapi's current API docs before any token issued by this code is used to start a real call.
- [ ] P2 — No real Vapi assistant exists in any environment yet (Step 30 built only the content templates in `modules/voice/profiles/`) — `docs/vapi-deployment.md`'s procedure needs a human with Vapi org access to run it; Step 31's token service now makes a created assistant reachable from the browser.
- [ ] P2 — `FEATURE_TAKEAWAY_REQUESTS`/`FEATURE_BOOKING_REQUESTS`/`FEATURE_EVENT_REQUESTS` remain unenforced by their guest-facing routes (found Step 24) — `FEATURE_VOICE_EN`/`FEATURE_VOICE_UR` are now the first flags actually read, by `/api/vapi/token` (Step 31); wire the other three the same way before launch.
- [ ] P2 — `release-gates-v2.md` Gate 5's "critical policy evals pass 100% in English, Urdu script, and Roman Urdu" needs every `requiresLiveModel: true` case in `modules/evals/cases/` (Step 29, D-033) actually run and passing — blocked on `ANTHROPIC_API_KEY` (same limitation as the item below). `cladium-research/agent/eval-policy.md` documents the review process for once a live key exists.
- [ ] P2 — Optional: raise Docker to ~7 GB for full-stack local work (Studio/Storage/Realtime/Edge Functions). Not needed for migrations — see `docs/database-environments.md`.
- [ ] P2 — A real Postgres/Supabase adapter for `lib/domain/`'s repository interfaces (D-023) is needed before Steps 19–25 can persist for real. Build once a live database connection is available (D-017). Must implement `findOrBegin`/`claimIfUnused`/`claimBatch` as single conditional writes (D-025/D-029).
- [ ] P2 — Build the takeaway cart/review UI pages once the menu is actually published (D-024) — the API is ready and waiting.
- [ ] P2 — Real staff authentication (Supabase Auth + owner/manager MFA linked to `staff_profiles`, Gate 3) is needed before Step 24's workspace is production-ready (D-028) — replaces `modules/staff/dev-credentials.ts` without any route changing, once a live Supabase project exists (D-017).
- [ ] P2 — A real Vercel Cron schedule for `/api/cron/outbox-dispatch` is a deployment-step (Step 46) concern, not built in Step 25 — the authenticated endpoint exists but nothing invokes it outside tests yet.
- [ ] P3 — Step 18 (menu carousel): explicitly deferred (D-022) until the menu is published and photos are owner-approved. Not a blocker for later steps.
- [ ] P3 — Local dev secrets (`SESSION_SECRET`, `NEXT_PUBLIC_APP_URL`, `CRON_SECRET`, `ANTHROPIC_API_KEY`) cannot be set in this sandbox (all `.env*` writes are denied by permission settings); a live happy-path check of the cart/CSRF/booking/event/staff/dispatch/concierge flow needs an environment without that restriction, or the user setting them directly.
- [ ] P2 — Found during Step 24: `FEATURE_TAKEAWAY_REQUESTS`/`FEATURE_BOOKING_REQUESTS`/`FEATURE_EVENT_REQUESTS` (`lib/env.server.ts`, added Step 6/12) are declared in the env schema but never read by `app/api/{takeaway,bookings,events}/*` routes (Steps 20–23) — the guest-facing request routes are always live regardless of these flags' values. Not a Step 24 fix (would touch unrelated Steps 20–23 files); wire `isFeatureEnabled(...)` into each `submit`/`review` route (or their `deps.ts`) before launch, defaulting closed to match `.env.example`'s current `false` values.

## Blocked for production, not safe scaffolding

- [ ] Owner menu and Urdu publication approval (menu content itself is structurally verified complete — see `scripts/validate` — this is a sign-off/translation task, not a content-completeness task).
- [ ] Vector/transparent logo and approved photo set.
- [ ] Staff accounts, roles, response/status procedures, MFA ownership.
- [ ] Tax/service-charge and collection-payment rules.
- [ ] Booking/treehouse/event operational details.
- [ ] Privacy, retention/deletion, consent, and legal wording.
- [ ] English/Urdu Vapi real-speaker bake-off.

## Completed

- [x] Research assets and verified operating knowledge collected.
- [x] Menu transcribed and structurally validated.
- [x] Architecture/data model/release gates Version 2 created.
- [x] Claude Code master prompt and gated runbook Version 2 created.
- [x] Legacy contradictions reconciled/deprecated.
- [x] Token-efficient context ledger and source router introduced.
- [x] Step 1 — baseline audit (facts, contradictions, media/gate risk register recorded).
- [x] Step 2 — ADRs 0001–0008 recorded under `cladium-research/architecture/adr/`.
- [x] Step 3 — deterministic validation tooling (`scripts/validate/`) added and run: full PASS; corrected the Step 1 menu-count bug; generated the owner sign-off report.
- [x] Local baseline committed (`8732db0`, not pushed) — research pack, governance, ADRs, validator tooling, placeholder-only `.env.example`.
- [x] Step 4 — Next.js App Router/TypeScript-strict scaffold: install, format, lint, typecheck, unit tests, source validators, and production build all pass; no business features built.
- [x] Step 5 — CI and repository hygiene: GitHub Actions mirrors the local verification chain; secret and client-bundle scans, contribution guidance, protected-branch recommendations, and dependency-update policy added. Full verification passes.
- [x] Step 6 — shared schemas and error conventions: strict boundary parsing, typed results/errors, correlation IDs, redacted structured logs, safe responses, and client/server environment separation. Full verification passes (102 unit tests).
- [x] Step 7 — Supabase local config, migration/generated-types workflow, environment-isolation docs, offline connection-routing check. Local `db reset` smoke passed (exit 0, PostgreSQL 17.6); reduced-stack command documented for low-memory Docker. No hosted project linked.
- [x] Step 8 — core content schema: 4 migrations, 12 tables, integer-PKR + timestamptz + version triggers, tri-state availability, approval-gated publishing, RLS enabled (default-deny) everywhere. Generated types committed. `db reset` clean, `db:test:schema` passes, offline migration lint in `verify`.
- [x] Step 9 — workflow schema: 4 migrations, 16 tables (28 total). Three state machines enforced by trigger, append-only history, immutable snapshot lines, single-use confirmation tokens, idempotency and webhook dedupe uniqueness, deferred staff FKs. `conversation_summaries` intentionally not created. `db reset` clean, schema tests pass.
- [x] Step 10 — RLS/grants/MFA policy: 3 migrations (helpers, policies, grants) covering all 28 tables; five staff roles plus anon/guest; `staff_requiring_mfa` and `public_business_settings` views; allow/deny matrix (`scripts/db/rls-tests.sql`) and offline migration lint (`scripts/db/migration-invariants.mjs`) added. `db reset` clean, `npm run db:test` (schema + RLS) and full `npm run verify` pass.

- [x] Step 11 — menu normalization and unpublished import planning: source adapter retained; pure draft import, source-checksum idempotency, human-reviewed Urdu translation candidates, stable-ID diff report, and approval-gated publish intent added. No database write or publishing occurs. 18 focused tests and the full `npm run verify` gate pass.
- [x] Step 12 — security foundation: signed opaque session cookies, CSRF/origin guards, safe redirects, request limits, CSP/security headers, provider-neutral rate/replay interfaces, HMAC webhook verification, safe 413/415 errors, and expanded redaction coverage. 36 focused security tests and full `npm run verify` pass.
- [x] Step 13 — locale routing: server-rendered `/en`/`/ur` under `app/[locale]/`, `proxy.ts` negotiation (cookie → Accept-Language → default, never an open redirect), signed preference cookie via `/api/locale-preference`, `lang`/`dir`/hreflang/`x-default`, and a `(root-fallback)` route group fix for a real `lang="en"`-on-Urdu-routes bug found via live browser check (D-018). 209 tests total; full `npm run verify` and manual browser check pass.
- [x] Step 14 — Day/Night design system: theme-mode.md tokens in `globals.css` (Day default; Night via `prefers-color-scheme` first-visit default and an explicit `data-theme` override); labelled, keyboard-accessible toggle (`theme-toggle.tsx`) that switches instantly client-side and persists via a deliberately unsigned, isomorphic cookie (D-019); server-aligned initial render (no flash) via `cookies()` in `[locale]/layout.tsx`; 180ms colour-only transition guarded by `prefers-reduced-motion`. 222 tests total; full `npm run verify` and manual browser check (both themes, both locales, instant switch, persistence) pass.
- [x] Step 15 — public shell and navigation: `SiteHeader`/`SiteFooter` (skip link, brand, minimal nav, hours + live timezone-aware open/closed status via `lib/business/hours.ts`); loading/not-found/error states inside the localized shell plus self-sufficient root `not-found`/`global-error` fallbacks; a catch-all route added after finding an unmatched-path 404 skipped the localized shell entirely (D-020). 233 tests total; full `npm run verify` and manual browser check (both locales/themes, both 404 paths, the error boundary, 360px RTL/LTR) pass.
- [x] Step 16 — home, location, and contact: `modules/business/facts.ts` expanded (address, map URL, WhatsApp, policy prose as `LocalizedText` — no invented Urdu); new `/visit` page (directions/map, hours+status, WhatsApp, policy list); light truthful Home update (tagline, Visit/WhatsApp CTAs); no photography or menu teaser (not approved/built yet). `primary-nav.tsx` added for a real nav active-state now that there are two real routes. 248 tests total; full `npm run verify` and manual browser check (both locales/themes, nav active-state, external-link attributes, 360px) pass.
- [x] Step 17 — accessible menu browsing: `modules/menu/menu-view.ts` (`PublishedMenuView` shape, `getPublishedMenuView()` always `UNPUBLISHED` today per D-021 — user-confirmed, matching release-gates-v2.md Gate 0/Gate 2/D-005 since owner sign-off is still outstanding); full search/category-filter/availability/PKR UI (`menu/page.tsx`) built as a no-JS `<form method="GET">`, live-verified against a temporary fixture then reverted; live `/menu` shows an honest not-yet-published state with WhatsApp/Visit fallbacks. 260 tests total; full `npm run verify` and manual browser check pass.
- [x] Step 18 — menu carousel: explicitly deferred, not built (D-022) — both stated preconditions (published menu, approved photos) are unmet.
- [x] Step 19 — domain repositories and state machines (D-023): `lib/domain/` provider-neutral primitives (actor/role authorization, generic state-machine + `performStaffTransition` orchestrator implementing data-model-v2.md §7's staff-transition contract, idempotency, single-use confirmation tokens, review-hash, append-only status/audit/outbox builders, optimistic-lock versioned store) — all dependency-injected, tested against in-memory reference stores (no live Postgres adapter yet). `modules/{takeaway,bookings,events}/state-machine.ts` encode the three diagrams exactly (role scope, guest-vs-staff transitions). `modules/takeaway/cart.ts` and `modules/{takeaway,bookings,events}/submission-service.ts` implement the full §7 submission transaction contract for all three request types. 292 new focused tests (exhaustive transition tables, idempotency, confirmation tokens, optimistic-lock races, full submission happy/idempotent-replay/stale-review paths). 526 tests total; full `npm run verify` passes. Nothing wired to a route yet — that's Steps 20–23.
- [x] Step 20 — takeaway draft and review (D-024): `lib/customer-session.ts` (guest session cookie, first real use of Step 12's primitive), `lib/http/{session-route,mutating-route,respond}.ts` (deterministic CSRF token + origin check, both fail closed on missing config; body-size/content-type checks; Result→NextResponse mapping), `modules/takeaway/{cart-store,http,deps,schemas}.ts`. Five routes: cart GET, items POST/PATCH/DELETE, review POST, submit POST. Cross-session access, price tampering, invalid quantity, and stale-menu are all structural, not runtime checks to remember. No cart/review UI page yet — the menu is still unpublished, so nothing exists for "Add to cart" to add. A real bug caught by its own test: the origin check originally required the full Supabase-inclusive client env schema, failing closed even for a valid CSRF token — fixed with a narrow `parseAppUrl` accessor. 32 new focused tests; 558 total; full `npm run verify` passes; live check confirmed the fail-closed path and no regression on existing pages.
- [x] Step 21 — takeaway submission (D-025): functional requirements were already done in Steps 19–20; Step 21's own evidence bullet ("concurrency, double-click, retry, rollback, atomicity") demanded a different test shape — genuine `Promise.all` concurrency, not sequential calls — which found and fixed a real race in `lib/domain/idempotency.ts` and `lib/domain/confirmation-token.ts` (two truly concurrent callers could both pass a "not started"/"not used" check before either wrote back). Fixed with one atomic store operation each (`findOrBegin`, `claimIfUnused`) instead of separate read-then-write calls. True multi-write atomicity/rollback is deferred to the real Postgres adapter (in-memory writes can't fail, so there's nothing to roll back yet). 5 new focused concurrency tests; 563 total; full `npm run verify` passes.
- [x] Step 22 — booking and treehouse requests (D-026): `modules/bookings/{deps,schemas,request-window}.ts`, `app/api/bookings/{review,submit}/route.ts`, `GET /api/session/csrf` (shared token bootstrap for forms with no natural GET-with-data endpoint), reusing Step 19's submission service and Step 20/21's session/CSRF/idempotency machinery unchanged. Unlike takeaway, a real UI was built (`app/[locale]/book/page.tsx` + `booking-form.tsx`: form → server-echoed review → confirmed), reachable from primary nav and two Home CTAs — buildable because booking has no dependency on the still-unpublished menu. 10 new focused tests (date-boundary + submission-service happy/idempotent/concurrency/bad-token); 573 total; full `npm run verify` passes including production build.
- [x] Step 23 — birthday and event enquiries (D-027): `modules/events/{deps,schemas}.ts`, `app/api/events/{review,submit}/route.ts`, `app/[locale]/event/page.tsx` + `event-form.tsx` (same form → review → confirmed shape as booking), on top of Step 19's already-built event submission service/state machine. `decorInterest` is a flag only — `quotedAmountPkr` stays `null`; no quote/confirmation service exists (staff-only, not built). `event/page.tsx` surfaces the approved décor/cake/outside-food policy wording, satisfying the runbook's own evidence requirement. Promoted `isTodayOrFutureDate` from `modules/bookings/` to `lib/business/request-window.ts` so events could reuse it without a cross-module import. Added the "Plan a Birthday" CTA to nav/Home. 5 new focused tests mirroring Step 22/21's concurrency shape; 578 total; full `npm run verify` passes including production build.
- [x] Step 24 — protected staff workspace (D-028): `performStaffAssignment` (new, metadata-only orchestrator) and mandatory-reason enforcement added to `lib/domain/staff-transition.ts`; `VersionedStore`/`AppendOnlySink` gained `list()`; `modules/{takeaway,bookings,events}/staff-service.ts` (deps-injected, mirroring `submission-service.ts`) give role-scoped queues, detail+history views, transitions, and assignment for all three entities. `app/staff/` (a third independent root layout) ships a real sign-in/queue/detail UI, English-only and unindexed. Real staff auth (Supabase Auth + MFA) is deferred to Gate 3 — a dev-only credential seam (`modules/staff/dev-credentials.ts` + optional `STAFF_DEV_ACCOUNTS`) stands in, fail-closed by default. AUDITOR can view but not write, per entity. Events' `QUOTED` transition requires `quotedAmountPkr`. 95 new focused tests (reason enforcement, assignment matrix, per-entity concurrent-transition races, AUDITOR view-only, quote requirement, session/cookie isolation, actor/CSRF guard matrix, fail-closed env parsing, `list()`); 658 total; full `npm run verify` passes including production build (all `/staff/*` pages + 13 new `/api/staff/*` routes) and a client-bundle scan; live check confirmed sign-in rendering, fail-closed 500 with `SESSION_SECRET` unconfigured, and a 404 for an unknown entity segment.
- [x] Step 25 — notifications and outbox dispatcher (D-029, Phase 4 complete): `OutboxEvent` gained `id`/`version`; `lib/domain/outbox-store.ts`'s atomic `claimBatch` (due/stale rows, oldest first) plus `markDelivered`/`markRetry`/`markTerminal`; `lib/domain/outbox-dispatcher.ts`'s `runDispatchCycle` (claim → handle → deliver/retry-with-backoff/terminal-at-maxAttempts). One shared outbox singleton (`modules/notifications/deps.ts`) replaces each entity's private sink, matching the single `outbox_events` table. The only handler, `modules/staff/notification-handlers.ts`, upserts by event id into `modules/staff/notification-store.ts` (idempotent), polled via `GET /api/staff/notifications` — Supabase Realtime deferred (D-017; ADR-0007: Realtime is a speed-up, not the guarantee). Trigger: `GET /api/cron/outbox-dispatch`, `Authorization: Bearer $CRON_SECRET`-authenticated (`parseCronSecret` + `lib/security/cron-auth.ts`, fail-closed). 34 new focused tests (worker overlap, crash/retry backoff, poison-message terminal handling, terminal-failure visibility, idempotent handler, cron-auth matrix); 684 total; full `npm run verify` passes including production build and a client-bundle scan; live check confirmed the cron endpoint fails closed (401) with no `CRON_SECRET`/header configured, no regression on `/staff`.
- [x] Step 26 — concierge policy and read tools (D-030, Phase 5 underway): `modules/concierge/policy.ts`'s `CONCIERGE_SYSTEM_POLICY` — one compact, cached, rules-only string sourced from `business/facts.ts`'s existing constants, never a fresh/duplicate copy of approved facts. Four strict read tools (`modules/concierge/tools/`): `getMenu` (reads `getPublishedMenuView`, still `UNPUBLISHED`), `getVenueInfo` (closed topic enum + live hours), `viewCart` (direct re-export of `modules/takeaway/http.ts`'s `getCart` — no second cart implementation), `getRequestStatus` (checks all three request stores, `{found:false}` for both "doesn't exist" and "wrong owner"). `viewCartInputSchema` is deliberately empty — session comes from the server, never the model. No live LLM wiring — no Anthropic SDK dependency exists yet and `ANTHROPIC_API_KEY` has never been read by any code; that's Step 27/29's job. 6 new test files (schema contracts, policy content, per-tool factual-correctness against approved constants); 737 total; full `npm run verify` passes; no new routes this step.
- [x] Step 27 — server-side chat orchestration (D-031): installed `@anthropic-ai/sdk`; `modules/integrations/anthropic-client.ts`'s provider-neutral `ChatClient` + real adapter (never live-called — no API key here; construction never throws, key read lazily inside `sendMessage`). `modules/concierge/orchestrator.ts`'s `orchestrateTurn`: rate-limit → bounded server-held history (never browser-supplied) → tool-call loop bounded by call count/cumulative tokens/wall-clock deadline → any failure mode resolves to the same safe WhatsApp-pointing fallback, never a leaked exception. `system` is always exactly `CONCIERGE_SYSTEM_POLICY`, structurally immune to guest input. `modules/concierge/tool-registry.ts` pairs each tool with a hand-written strict JSON schema; unknown tool name is `NOT_FOUND`, schema-invalid input is a self-correctable tool-result error. No chat UI yet — deferred to Step 28. 4 new test files covering the runbook's evidence bullet exactly (cross-session isolation, prompt-injection guarantees, loop exhaustion, timeout, PII-safe telemetry, cost limit); 779 total; full `npm run verify` passes including production build listing `/api/concierge/chat`; live check confirmed fail-closed 500 with `SESSION_SECRET` unconfigured.
- [x] Step 28 — concierge draft actions and confirmation (D-032): `prepareBookingRequest`/`prepareEventRequest` concierge tools wrap Step 19's existing prepare functions exactly, input schemas reusing `modules/{bookings,events}/schemas.ts`'s exported draft-field validators field-for-field. No `prepareTakeawayRequest` (menu still unpublished, same D-021/D-024 reasoning). No submit tool is ever registered in the tool registry — structurally, not by convention; only a real chat-UI Confirm tap calls `POST /api/{bookings,events}/submit` directly, the exact manual-form endpoints. `orchestrateTurn` gained `pendingConfirmation`, surfacing a successful prepare call's review+token structurally for the UI; only the latest prepare call in a turn wins and it survives a later same-turn failure. Real chat UI shipped (`/concierge`) — CLAUDE.md's "Ask Cladium Concierge" CTA. 4 test files new/extended, including an end-to-end proof a concierge token really submits through the real function and that tampering still fails `STALE_REVIEW`; 805 total; full `npm run verify` passes including production build listing `/[locale]/concierge` and `/api/concierge/chat`; live check confirmed correct rendering and fail-closed behavior.
- [x] Step 29 — agent evaluation harness (D-033, Phase 5 complete): `modules/evals/{types,assertions,runner}.ts` run every case through the real `orchestrateTurn` against a scripted fake `ChatClient` — never a reimplementation. `requiresLiveModel: false` cases run now deterministically (real tool dispatch, including the real Step 19 submission service — e.g. `reviewNeverQuotesAPrice()` checks a genuinely prepared `EventReview`, not case-authored text); `requiresLiveModel: true` cases (bilingual comprehension, ambiguous tool selection, live injection resistance) are visibly `skipped` with a reason, never faked — no `ANTHROPIC_API_KEY` here (D-031). `modules/evals/cases/` covers all 11 runbook-named categories, 27 cases (12 skipped, 15 run and pass, all runnable cases `critical: true`). `tests/unit/evals.test.ts` is the CI gate; `cladium-research/agent/eval-policy.md` documents the threshold/review process. 6 new tests; 810 total (66 files); full `npm run verify` passes; no new route.
- [x] Step 30 — Vapi configuration as controlled artifacts (D-034, Phase 6 underway): `modules/voice/profiles/{types,policy,templates,validate}.ts` — a six-cell `VAPI_PROFILE_MATRIX` (development/preview/production × en/ur) with no assistant id/org id/credential field at all, structurally (same "absence, not convention" pattern as Step 28's missing submit tool); real ids/secrets stay in server env vars, read only by Steps 31–32, not this module. `buildVoiceSystemPrompt` wraps `CONCIERGE_SYSTEM_POLICY` verbatim plus a voice-conduct addendum — never a duplicate copy. `POLICY_VERSIONS`/`TOOL_SCHEMA_VERSIONS` are fingerprint lockfiles implementing "shared policy/tool schema versions" as real drift detection (a test recomputes the live SHA-256 fingerprint and fails on mismatch). `voiceStack` defaults every cell to `PENDING_BAKEOFF` — a real selection needs Step 34's evidence. `firstMessage`/`closingMessage` render English-only in both locales (stricter than `chrome.ts`'s reviewed-UI-copy bar — this is spoken audio, unreviewed by a fluent speaker yet). `docs/vapi-deployment.md` (environment table + deployment procedure + promotion order) and `modules/voice/profiles/CHANGELOG.md` cover the "change logs, and a deployment procedure" wording. 17 new focused tests; 827 total; full `npm run verify` passes; no new route (no live Vapi call yet).
- [x] Step 31 — short-lived browser token service (D-035, Phase 6 underway): `lib/security/jwt.ts` (hand-rolled spec-shaped compact HS256 JWT, `node:crypto` only, no new dependency — a real RFC 7519 wire format since Vapi itself verifies it). `modules/integrations/vapi-client.ts#createVapiTokenIssuer` mirrors `createAnthropicChatClient`'s never-throws-at-construction shape; its claim shape is a best-effort mapping to Vapi's documented format, explicitly flagged as unverified against a live credential. `modules/voice/token/issue-vapi-token.ts#issueVapiToken` is the orchestration — feature-flag check first (`FEATURE_VOICE_EN`/`FEATURE_VOICE_UR`, the first `FEATURE_*` flag actually enforced in this codebase), then a dedicated 5/min rate limit, then issuance; a thrown credential error never leaks its message. `POST /api/vapi/token` reuses Step 20's `parseMutatingRequest` guard unchanged and always embeds the server's own configured origin, never a guest header value. 24 new focused tests; 851 total; full `npm run verify` passes including production build listing `/api/vapi/token`; no live Vapi credential exists here, so only the fail-closed/validation paths are exercised.

## Update rule

Keep only actionable work here. Move completed items to the short Completed list; put rationale in `DECISIONS.md`, not in task descriptions. After any completed runbook step, refresh the step-completion snapshot (overall/phase, as percentages — not effort) in `PROJECT_STATE.md`'s `## Progress` section; see `CLAUDE.md` Workflow rules for the rule itself.
