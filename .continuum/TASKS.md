# Cladium task ledger

## Active

- [ ] P1 — Blocked on GO: Step 46 (controlled production deployment) explicitly requires an approved GO first (release-gates-v2.md's own rule) — Step 45's decision (D-049) was **NO-GO**. Active work now is resolving the punch list D-049/`production-readiness-decision.md` lays out (split into owner/business items and engineering items below), then re-running Step 45's evaluation for a fresh go/no-go before Step 46 can begin.

## Next

- [ ] P2 — `modules/integrations/vapi-client.ts`'s restricted-JWT claim shape and `modules/voice/webhook-auth.ts`'s header names remain best-effort/unverified against a live credential (Steps 31–32, D-035/D-036) — Step 33 confirmed the tool-calls envelope shape and client-SDK event names against `@vapi-ai/web`'s real types, narrowing what's still open. Confirm the remaining two against Vapi's current API docs/Custom Credential configuration before any of this code handles a real call.
- [ ] P2 — No real Vapi assistant exists in any environment yet (Step 30 built only the content templates in `modules/voice/profiles/`) — `docs/vapi-deployment.md`'s procedure needs a human with Vapi org access to run it; Step 31's token service now makes a created assistant reachable from the browser.
- [x] P2 — `FEATURE_TAKEAWAY_REQUESTS`/`FEATURE_BOOKING_REQUESTS`/`FEATURE_EVENT_REQUESTS` remain unenforced by their guest-facing routes (found Step 24) — wired (D-051), see the Completed entry below.
- [ ] P2 — `release-gates-v2.md` Gate 5's "critical policy evals pass 100% in English, Urdu script, and Roman Urdu" needs every `requiresLiveModel: true` case in `modules/evals/cases/` (Step 29, D-033) actually run and passing — blocked on `ANTHROPIC_API_KEY` (same limitation as the item below). `cladium-research/agent/eval-policy.md` documents the review process for once a live key exists.
- [ ] P2 — Optional: raise Docker to ~7 GB for full-stack local work (Studio/Storage/Realtime/Edge Functions). Not needed for migrations — see `docs/database-environments.md`.
- [ ] P2 — A real Postgres/Supabase adapter for `lib/domain/`'s repository interfaces (D-023) is needed before Steps 19–25 can persist for real. Build once a live database connection is available (D-017). Must implement `findOrBegin`/`claimIfUnused`/`claimBatch` as single conditional writes (D-025/D-029).
- [ ] P2 — Build the takeaway cart/review UI pages once the menu is actually published (D-024) — the API is ready and waiting.
- [x] P2 — Real staff authentication (Supabase Auth + owner/manager MFA linked to `staff_profiles`, Gate 3) built (D-050) — `modules/staff/dev-credentials.ts` is superseded, not removed: `createCompositeStaffDirectory` offers both, and no caller of `StaffDirectory` changed, exactly as this item originally specified. Real *accounts* still need provisioning — see the two items below.
- [ ] P2 — A real Vercel Cron schedule for `/api/cron/outbox-dispatch` is a deployment-step (Step 46) concern, not built in Step 25 — the authenticated endpoint exists but nothing invokes it outside tests yet.
- [ ] P3 — Step 18 (menu carousel): explicitly deferred (D-022) until the menu is published and photos are owner-approved. Not a blocker for later steps.
- [ ] P3 — Local dev secrets (`SESSION_SECRET`, `NEXT_PUBLIC_APP_URL`, `CRON_SECRET`, `ANTHROPIC_API_KEY`) cannot be set in this sandbox (all `.env*` writes are denied by permission settings); a live happy-path check of the cart/CSRF/booking/event/staff/dispatch/concierge flow needs an environment without that restriction, or the user setting them directly.
- [x] P2 — Found during Step 24: `FEATURE_TAKEAWAY_REQUESTS`/`FEATURE_BOOKING_REQUESTS`/`FEATURE_EVENT_REQUESTS` (`lib/env.server.ts`, added Step 6/12) are declared in the env schema but never read by `app/api/{takeaway,bookings,events}/*` routes (Steps 20–23) — wired (D-051): all 8 mutating routes plus `GET /api/takeaway/cart` now fail closed to `404 FEATURE_DISABLED` when their entity's flag is off.
- [ ] P3 — The Step 36 migration (`20260829150001_consent_retention.sql`, `purge_expired_consent_events`/`forbid_consent_row_change`) is offline-lint-checked only (`npm run check:db-config` passes) — its transaction-local-flag DELETE exception has still not been proven against a real `consent_events` table specifically. Step 42 confirmed local Docker/Supabase IS reachable in this sandbox (see below) and ran `db:test:schema`/`db:test:rls` live, but did not exercise this specific function — verify with a manual `select purge_expired_consent_events(1);` the next time the local stack is up.
- [ ] P2 — Step 37 left `contact`/`lead` Meta events fully built and tested (`modules/integrations/meta-events.ts`, `POST /api/meta/track`, `lib/analytics/meta-track-client.ts`) but not click-wired into the three existing WhatsApp links (Step 35, `page.tsx`/`visit/page.tsx`/`menu/page.tsx`/`privacy/page.tsx`) — doing so needs converting those already-shipped, already-tested plain `<a>` tags into one shared client component (an `onClick` handler needs a client boundary), which also needs `tests/unit/external-links.test.ts` updated to match. A deliberate, separable follow-up (D-041), not a forgotten gap.
- [ ] P2 — Step 37 deliberately did not inject the actual browser Meta Pixel `<script>` bootstrap into any page. Step 40 wired `lib/security/headers.ts`'s CSP into every real (non-dev) response via `next.config.ts`, but `script-src`/`connect-src` still have no allowance for `connect.facebook.net`/`www.facebook.com`. Before Meta measurement can go fully live: add an inline Pixel bootstrap snippet (only when `FEATURE_META_MARKETING` is on and `META_PIXEL_ID` is configured) and pass those two Meta domains into `securityHeaders({ connectSrc: [...] })`'s already-built extension point — needs its own focused review since it's a real security-header change, and this sandbox has no real `META_PIXEL_ID` to verify the loaded script against anyway.
- [ ] P2 — Step 40 wired CSP/HSTS/X-Frame-Options/Permissions-Policy/Referrer-Policy into every real (non-dev) response (`next.config.ts` → `lib/security/headers.ts#securityHeaders()`), verified against a real `next build && next start` server (full render/hydration, headers present, no console errors) — but it is a *static* application with no per-request nonce, so `script-src`/`style-src` include `'unsafe-inline'` (the honest baseline Next.js's own inline hydration scripts need without one). A real nonce needs every page to opt into dynamic rendering (Next's own documented requirement) — this app still has three statically-rendered routes (`/`, `/_not-found`, `/staff`). Converting those to remove `'unsafe-inline'` is a genuine, separately-scoped architectural decision (loses static generation/ISR for the whole site) — do not fold it into an unrelated future step without deliberate review.
- [ ] P2 — Step 40 found `securityHeaders()`'s CSP `connect-src` is `'self'` only — before `FEATURE_VOICE_EN`/`FEATURE_VOICE_UR` are enabled in any real environment, confirm Vapi's actual required browser-SDK origins (`@vapi-ai/web` connects out directly from the browser) against Vapi's current docs and add them via `securityHeaders({ connectSrc: [...] })`, or voice calls will fail silently under CSP the moment a real Vapi credential exists — this was never caught before because no real credential has existed in this sandbox to exercise it (D-035/D-036).
- [ ] P2 — Step 40's E2E rate-limit coverage (`tests/e2e/rate-limiting.spec.ts`) exercises only the guest-mutation routes and the staff sign-in credential-guessing surface; already-authenticated staff transition/assignment routes (`app/api/staff/{takeaway,bookings,events}/[id]/{transition,assign}`) deliberately received no new rate limit this step — real abuse there is a compromised-account problem a request throttle doesn't solve, and adding one broadly was judged out of scope for this step's diff. Revisit only if a concrete abuse pattern against those routes is ever observed.
- [ ] P2 — Found during Step 39's E2E run, real and reproduced (not fixed — needs a proxy-level check, per Next.js's own team guidance, not a page-level one): `GET /en/<unmatched-path>` (the catch-all `[locale]/[...rest]/page.tsx`'s `notFound()`, Step 15) returns HTTP `200`, not `404` — confirmed against both `next dev` and a production `next build && next start`. Root cause is a well-documented Next.js App Router limitation (streaming has already flushed a `200` status before the `notFound()` boundary swap happens, whenever a Suspense boundary — here, `[locale]/loading.tsx` — sits in the render path); Vercel/Next.js's own suggested fix is to run the not-found check in `proxy.ts` (this project's Step 13 middleware-equivalent) before rendering begins, not inside the React tree. The actual guest-visible content (correct localized shell, correct not-found copy, no crash) is already verified correct — only the HTTP status code is wrong. `tests/e2e/errors.spec.ts` documents this precisely and asserts only the content, not the status, for this one path.
- [ ] P3 — Found during Step 39: `[locale]/layout.tsx`'s `generateMetadata` builds canonical/hreflang alternates from the locale segment only (`localeMetadataAlternates(locale)`, no sub-path) — every nested page (e.g. `/en/visit`) reports the locale-root canonical/hreflang, not a per-page-accurate one. Gate 7's own bullet ("`/en` and `/ur` pages are server-rendered with correct... canonical, hreflang...") is satisfied by the current behavior, but true per-page SEO accuracy would need `generateMetadata` added to each page individually — a real, separate improvement, not expanded into Step 39's own scope. `tests/e2e/locale-metadata.spec.ts` documents the current behavior explicitly.
- [ ] P2 — Step 41's `performance-resilience-report.md` proposes concrete alert thresholds (outbox terminal-failure rate, guest-route 429 rate, provider-timeout rate, concierge turn-deadline-hit rate) as engineering-judgment starting points — no live monitoring/alerting stack exists to actually wire them into yet. Configure real alerts against these thresholds once a staging/production observability stack exists (Step 43/46).
- [ ] P2 — Step 41 found Core Web Vitals cannot be meaningfully measured in this sandbox (Gate 7 itself scopes the real P75 measurement to staging/production telemetry) — only a local `localhost` proxy measurement exists (FCP/LCP ~164ms, CLS 0, all four pages tested). Re-measure for real once Step 43's staging environment exists, and again once real hero photography ships (today's identical FCP/LCP across pages reflects the current text-only content, not a representative future measurement).
- [ ] P2 — Step 41 found and fixed a real concurrency bug in `lib/security/rate-limit.ts`'s in-memory adapter (returned a live mutable object instead of a value snapshot, silently breaking under a genuine `Promise.all` burst — see D-045). The fix is verified at volume in `tests/unit/performance-load.test.ts`, but this remains the *in-memory, dev-only* adapter (D-023) — whoever builds the real production `RateLimitStoreAdapter` (Redis/Supabase-backed) should re-read D-045's reasoning before implementing, since the same "return a live reference instead of a snapshot" mistake could reappear in a different shape there.
- [ ] P2 — Step 41's load tests only exercise the in-memory reference stores (D-023) — real Postgres/Supabase contention (connection-pool exhaustion, real lock wait times, real query planning under load) remains unmeasured pending a live database adapter and a live Supabase project (D-017), same standing gap as every prior step.
- [x] P1 — **Capability update (Step 42)**: local Docker + the Supabase CLI's reduced stack (`docs/database-environments.md`'s documented low-memory profile) IS reachable in this sandbox — confirmed live, not assumed. This does **not** resolve D-017 (no *hosted* Supabase project, no staging/production Supabase Pro, no real PITR/backup system) but it does mean `npm run db:test:schema`/`db:test:rls`/a real `db reset` can be run live in this sandbox going forward, if Docker Desktop is started first (`Start-Process 'C:\Program Files\Docker\Docker\Docker Desktop.exe'`, then poll `docker ps` — took under 30s this step). Re-check this capability before assuming any DB-related task is blocked purely on "no live Postgres."
- [ ] P2 — Step 42 found and fixed a real, previously-undiscovered gap: every table (`confirmation_tokens`/`idempotency_keys` included) carried full `anon`/`authenticated` CRUD grants via a Supabase platform default `20260824140003_grants.sql` never revoked — undetected through 31 runbook steps because `db:test:rls` had no live database to run against. Fixed in `20260830044140_fix_default_table_privileges.sql` (D-046). The next migration author should re-read that migration's own doc comment before adding any new table — Supabase's default-grant behavior applies to *every new table* unless the `ALTER DEFAULT PRIVILEGES` this migration set up continues to hold (verify it survives any future `db reset`, not just this step's).
- [ ] P2 — Step 42's backup/restore drill used generic `pg_dump`/`psql` against local Postgres, not Supabase Pro's actual managed PITR/backup system (which is WAL-based and dashboard/API-driven, operationally different). Re-run a real timed drill against an actual Supabase Pro project's backup/restore mechanism once one exists (Step 43/45) — do not treat this step's ~11s local RTO as a production estimate; see `backup-restore-rollback-drill-report.md`'s proposed 30 min RTO / 5 min RPO instead.
- [ ] P2 — Step 42's proposed RTO (30 min) / RPO (5 min) recovery objectives, and Gate 9's named launch owner/incident contact/rollback decision-maker, need real owner review and real names — not something this build process can supply. Confirm/adjust at Step 44 (UAT) or Step 45 (production readiness decision).
- [ ] P2 — Step 43 found a real Vercel deployment gotcha, worth carrying into a future deployment runbook (or `docs/database-environments.md`) rather than only living in `staging-release-report.md`: an environment-variable change that affects request-time behavior (e.g. `NEXT_PUBLIC_APP_URL`) needs a genuinely fresh rebuild to take effect — clicking "Redeploy" while reusing the existing build cache can silently leave the old value active, which manifested as every mutating request failing closed with `403` until an explicit cache-free rebuild was triggered.
- [ ] P2 — Step 43 deployed staging on Vercel **Hobby**, an explicit owner decision — upgrade to **Pro** is required before commercial/production use (Gate 9, `deployment-target.md`). Confirm this happens before Step 46 (controlled production deployment).
- [x] P3 — Step 43 left real test data in the staging database (two bookings, one event request) — checked live against the hosted project (`vxvpxywszskxcugwpsch`) before Step 44: `booking_requests`/`event_requests`/`status_events`/`audit_events`/`outbox_events` are all already empty, none of the three Step 43 IDs remain. No delete was needed; queues are already clean for UAT.
- [ ] P2 — Step 43 proved the real Anthropic integration works end to end for the first time in this project (a real chat reply, correct hours/status/WhatsApp pointer) — but only for one simple factual question. The fuller adversarial/policy eval suite (`modules/evals/cases/`, Step 29, D-033) still has every `requiresLiveModel: true` case unrun — now that a real key exists in at least one environment, this is a good moment to actually run those against staging rather than leaving them permanently skipped (Gate 5's own requirement).
- [ ] P3 — Step 43 considered and rejected a third-party "AI API gateway" relay (`api.mwapi.dev`) for the staging Anthropic key — not because the underlying idea is inherently wrong, but because that specific service's recommended setup was a blind `irm | iex` script execution (blocked by Claude Code's own safety classifier) and its test keys returned `401 INVALID_API_KEY` against the real Messages API shape. If cost becomes a real concern later, evaluate a reputable, audited alternative deliberately and separately — never via an unreviewed install script.
- [ ] P3 — Step 44 left one real (clearly fake, "TEST — UAT Step 44") booking and one event enquiry in the staging database, plus a "Step 44 UAT test transition" audit entry on the booking — same pattern as Step 43's test data, harmless and not blocking. Worth clearing before a genuine independent staff walkthrough or before Step 46, whichever comes first, if a clean slate is wanted.
- [ ] P2 — Step 44 added a third `STAFF_DEV_ACCOUNTS` entry (`order_staff`, `ORDER_STAFF`) to staging to make role-scoping actually testable (D-048) — confirmed working (403s on out-of-scope queues, no crash). All three dev accounts (owner/manager/order_staff) are scaffolding only (`modules/staff/dev-credentials.ts`, D-028) and must never exist in any production `STAFF_DEV_ACCOUNTS` value — superseded entirely once real Supabase Auth (Gate 3) ships, same standing requirement as before, just now covering one more account.
- [ ] P1 — Step 44's owner/staff UAT (D-048) was run as a live collaborative session (Claude driving the real staging site/database under the owner's direction and sign-off), not independent owner/staff testing on their own devices — release-gates-v2.md's UAT gate still ultimately expects that broader round before production. Schedule a genuine walkthrough with each real staff role on their own phone before Step 46.
- [ ] P2 — Step 44 left the notification response procedure (who checks for new requests, how often, what happens if nobody responds in time) undecided — an owner staffing decision, not something this build process can answer. Needed before Step 45's go/no-go.
- [ ] P2 — Step 44 did not collect real names for Gate 9's launch owner / incident contact / rollback decision-maker / monitoring window (Section 12 of the UAT checklist) — same standing item as D-046's proposed RTO/RPO. Needed before Step 45's go/no-go.
- [x] P1 — Step 45 (D-049): the one hard technical blocker to GO — real Supabase Auth + owner/manager MFA. Built (D-050): the code path is real, tested, and structurally enforces MFA for OWNER/MANAGER. Still open: real *accounts* need provisioning (owner action) before this gate can actually pass — see the two items below and `cladium-research/operations/real-staff-account-setup.md`.
- [ ] P1 — Step 45/D-050 follow-up: no real Supabase Auth **accounts** exist yet in staging — the code path is built and tested, but Gate 3 cannot actually be marked passed until at least one real owner/manager account is created, linked to a `staff_profiles` row, and has completed real MFA enrollment. Procedure: `cladium-research/operations/real-staff-account-setup.md`. Needs the owner directly (account creation/password choice is a prohibited action for this build process to perform).
- [ ] P3 — Step 45/D-050 follow-up: once real accounts are provisioned and confirmed working in staging, remove `STAFF_DEV_ACCOUNTS` from staging's Vercel env vars — it must never remain set once real accounts cover actual usage, same standing rule as always, now actually actionable.
- [x] P1 — Step 45 (D-049) re-examined the unenforced `FEATURE_TAKEAWAY_REQUESTS`/`FEATURE_BOOKING_REQUESTS`/`FEATURE_EVENT_REQUESTS` flags (open since Step 24) specifically against Gate 1's "reject direct calls to disabled routes" wording and confirmed it's a real gate failure — wired (D-051).
- [ ] P2 — Step 45 (D-049) new finding: the Supabase project is region `ap-northeast-1` (Tokyo); Vercel's function region has never been checked or explicitly configured against it. Verify alignment (and set explicitly if needed, e.g. `hnd1`) before the Pro upgrade/production deploy — a real latency risk if left mismatched.
- [ ] P2 — Step 45 (D-049) new finding: `npm audit` has no automated CI step — Step 40's "0 vulnerabilities" was a one-time manual check, never repeated or automated since. Add it to `verify`/CI as a real, repeatable dependency-scan gate before launch.
- [ ] P2 — Step 45 (D-049): re-run the backup/restore drill (Step 42) and the load/concurrency tests (Step 41) against real Supabase Pro once upgraded — both are currently proven only at the local-Postgres/in-memory layer, not the production-grade managed mechanisms.

## Blocked for production, not safe scaffolding

- [ ] Owner menu and Urdu publication approval (menu content itself is structurally verified complete — see `scripts/validate` — this is a sign-off/translation task, not a content-completeness task).
- [ ] Vector/transparent logo and approved photo set.
- [ ] Staff accounts, roles, response/status procedures, MFA ownership.
- [ ] Tax/service-charge and collection-payment rules.
- [ ] Booking/treehouse/event operational details.
- [ ] Privacy, retention/deletion, consent, and legal wording.
- [ ] English/Urdu Vapi real-speaker bake-off (Step 34, D-038) — test plan/protocol fully prepared and ready (`cladium-research/operations/voice-bake-off-plan.md`), execution blocked on a live Vapi credential/deployed assistants plus recruited, consented real Pakistani English/Urdu speakers.

## Completed

- [x] Research assets and verified operating knowledge collected.
- [x] Menu transcribed and structurally validated.
- [x] Architecture/data model/release gates Version 2 created.
- [x] Claude Code master prompt and gated runbook Version 2 created.
- [x] Legacy contradictions reconciled/deprecated.
- [x] Token-efficient context ledger and source router introduced.
- [x] Step 1 — baseline audit (facts, contradictions, media/gate risk register recorded).
- [x] Step 2 — ADRs 0001–0008 recorded under `cladium-research/architecture/adr/`.
- [x] Step 3 — deterministic validation tooling (`scripts/validate/`) added and run: full PASS; corrected the Step 1 menu-count bug; generated the owner sign-off report.
- [x] Local baseline committed (`8732db0`, not pushed) — research pack, governance, ADRs, validator tooling, placeholder-only `.env.example`.
- [x] Step 4 — Next.js App Router/TypeScript-strict scaffold: install, format, lint, typecheck, unit tests, source validators, and production build all pass; no business features built.
- [x] Step 5 — CI and repository hygiene: GitHub Actions mirrors the local verification chain; secret and client-bundle scans, contribution guidance, protected-branch recommendations, and dependency-update policy added. Full verification passes.
- [x] Step 6 — shared schemas and error conventions: strict boundary parsing, typed results/errors, correlation IDs, redacted structured logs, safe responses, and client/server environment separation. Full verification passes (102 unit tests).
- [x] Step 7 — Supabase local config, migration/generated-types workflow, environment-isolation docs, offline connection-routing check. Local `db reset` smoke passed (exit 0, PostgreSQL 17.6); reduced-stack command documented for low-memory Docker. No hosted project linked.
- [x] Step 8 — core content schema: 4 migrations, 12 tables, integer-PKR + timestamptz + version triggers, tri-state availability, approval-gated publishing, RLS enabled (default-deny) everywhere. Generated types committed. `db reset` clean, `db:test:schema` passes, offline migration lint in `verify`.
- [x] Step 9 — workflow schema: 4 migrations, 16 tables (28 total). Three state machines enforced by trigger, append-only history, immutable snapshot lines, single-use confirmation tokens, idempotency and webhook dedupe uniqueness, deferred staff FKs. `conversation_summaries` intentionally not created. `db reset` clean, schema tests pass.
- [x] Step 10 — RLS/grants/MFA policy: 3 migrations (helpers, policies, grants) covering all 28 tables; five staff roles plus anon/guest; `staff_requiring_mfa` and `public_business_settings` views; allow/deny matrix (`scripts/db/rls-tests.sql`) and offline migration lint (`scripts/db/migration-invariants.mjs`) added. `db reset` clean, `npm run db:test` (schema + RLS) and full `npm run verify` pass.

- [x] Step 11 — menu normalization and unpublished import planning: source adapter retained; pure draft import, source-checksum idempotency, human-reviewed Urdu translation candidates, stable-ID diff report, and approval-gated publish intent added. No database write or publishing occurs. 18 focused tests and the full `npm run verify` gate pass.
- [x] Step 12 — security foundation: signed opaque session cookies, CSRF/origin guards, safe redirects, request limits, CSP/security headers, provider-neutral rate/replay interfaces, HMAC webhook verification, safe 413/415 errors, and expanded redaction coverage. 36 focused security tests and full `npm run verify` pass.
- [x] Step 13 — locale routing: server-rendered `/en`/`/ur` under `app/[locale]/`, `proxy.ts` negotiation (cookie → Accept-Language → default, never an open redirect), signed preference cookie via `/api/locale-preference`, `lang`/`dir`/hreflang/`x-default`, and a `(root-fallback)` route group fix for a real `lang="en"`-on-Urdu-routes bug found via live browser check (D-018). 209 tests total; full `npm run verify` and manual browser check pass.
- [x] Step 14 — Day/Night design system: theme-mode.md tokens in `globals.css` (Day default; Night via `prefers-color-scheme` first-visit default and an explicit `data-theme` override); labelled, keyboard-accessible toggle (`theme-toggle.tsx`) that switches instantly client-side and persists via a deliberately unsigned, isomorphic cookie (D-019); server-aligned initial render (no flash) via `cookies()` in `[locale]/layout.tsx`; 180ms colour-only transition guarded by `prefers-reduced-motion`. 222 tests total; full `npm run verify` and manual browser check (both themes, both locales, instant switch, persistence) pass.
- [x] Step 15 — public shell and navigation: `SiteHeader`/`SiteFooter` (skip link, brand, minimal nav, hours + live timezone-aware open/closed status via `lib/business/hours.ts`); loading/not-found/error states inside the localized shell plus self-sufficient root `not-found`/`global-error` fallbacks; a catch-all route added after finding an unmatched-path 404 skipped the localized shell entirely (D-020). 233 tests total; full `npm run verify` and manual browser check (both locales/themes, both 404 paths, the error boundary, 360px RTL/LTR) pass.
- [x] Step 16 — home, location, and contact: `modules/business/facts.ts` expanded (address, map URL, WhatsApp, policy prose as `LocalizedText` — no invented Urdu); new `/visit` page (directions/map, hours+status, WhatsApp, policy list); light truthful Home update (tagline, Visit/WhatsApp CTAs); no photography or menu teaser (not approved/built yet). `primary-nav.tsx` added for a real nav active-state now that there are two real routes. 248 tests total; full `npm run verify` and manual browser check (both locales/themes, nav active-state, external-link attributes, 360px) pass.
- [x] Step 17 — accessible menu browsing: `modules/menu/menu-view.ts` (`PublishedMenuView` shape, `getPublishedMenuView()` always `UNPUBLISHED` today per D-021 — user-confirmed, matching release-gates-v2.md Gate 0/Gate 2/D-005 since owner sign-off is still outstanding); full search/category-filter/availability/PKR UI (`menu/page.tsx`) built as a no-JS `<form method="GET">`, live-verified against a temporary fixture then reverted; live `/menu` shows an honest not-yet-published state with WhatsApp/Visit fallbacks. 260 tests total; full `npm run verify` and manual browser check pass.
- [x] Step 18 — menu carousel: explicitly deferred, not built (D-022) — both stated preconditions (published menu, approved photos) are unmet.
- [x] Step 19 — domain repositories and state machines (D-023): `lib/domain/` provider-neutral primitives (actor/role authorization, generic state-machine + `performStaffTransition` orchestrator implementing data-model-v2.md §7's staff-transition contract, idempotency, single-use confirmation tokens, review-hash, append-only status/audit/outbox builders, optimistic-lock versioned store) — all dependency-injected, tested against in-memory reference stores (no live Postgres adapter yet). `modules/{takeaway,bookings,events}/state-machine.ts` encode the three diagrams exactly (role scope, guest-vs-staff transitions). `modules/takeaway/cart.ts` and `modules/{takeaway,bookings,events}/submission-service.ts` implement the full §7 submission transaction contract for all three request types. 292 new focused tests (exhaustive transition tables, idempotency, confirmation tokens, optimistic-lock races, full submission happy/idempotent-replay/stale-review paths). 526 tests total; full `npm run verify` passes. Nothing wired to a route yet — that's Steps 20–23.
- [x] Step 20 — takeaway draft and review (D-024): `lib/customer-session.ts` (guest session cookie, first real use of Step 12's primitive), `lib/http/{session-route,mutating-route,respond}.ts` (deterministic CSRF token + origin check, both fail closed on missing config; body-size/content-type checks; Result→NextResponse mapping), `modules/takeaway/{cart-store,http,deps,schemas}.ts`. Five routes: cart GET, items POST/PATCH/DELETE, review POST, submit POST. Cross-session access, price tampering, invalid quantity, and stale-menu are all structural, not runtime checks to remember. No cart/review UI page yet — the menu is still unpublished, so nothing exists for "Add to cart" to add. A real bug caught by its own test: the origin check originally required the full Supabase-inclusive client env schema, failing closed even for a valid CSRF token — fixed with a narrow `parseAppUrl` accessor. 32 new focused tests; 558 total; full `npm run verify` passes; live check confirmed the fail-closed path and no regression on existing pages.
- [x] Step 21 — takeaway submission (D-025): functional requirements were already done in Steps 19–20; Step 21's own evidence bullet ("concurrency, double-click, retry, rollback, atomicity") demanded a different test shape — genuine `Promise.all` concurrency, not sequential calls — which found and fixed a real race in `lib/domain/idempotency.ts` and `lib/domain/confirmation-token.ts` (two truly concurrent callers could both pass a "not started"/"not used" check before either wrote back). Fixed with one atomic store operation each (`findOrBegin`, `claimIfUnused`) instead of separate read-then-write calls. True multi-write atomicity/rollback is deferred to the real Postgres adapter (in-memory writes can't fail, so there's nothing to roll back yet). 5 new focused concurrency tests; 563 total; full `npm run verify` passes.
- [x] Step 22 — booking and treehouse requests (D-026): `modules/bookings/{deps,schemas,request-window}.ts`, `app/api/bookings/{review,submit}/route.ts`, `GET /api/session/csrf` (shared token bootstrap for forms with no natural GET-with-data endpoint), reusing Step 19's submission service and Step 20/21's session/CSRF/idempotency machinery unchanged. Unlike takeaway, a real UI was built (`app/[locale]/book/page.tsx` + `booking-form.tsx`: form → server-echoed review → confirmed), reachable from primary nav and two Home CTAs — buildable because booking has no dependency on the still-unpublished menu. 10 new focused tests (date-boundary + submission-service happy/idempotent/concurrency/bad-token); 573 total; full `npm run verify` passes including production build.
- [x] Step 23 — birthday and event enquiries (D-027): `modules/events/{deps,schemas}.ts`, `app/api/events/{review,submit}/route.ts`, `app/[locale]/event/page.tsx` + `event-form.tsx` (same form → review → confirmed shape as booking), on top of Step 19's already-built event submission service/state machine. `decorInterest` is a flag only — `quotedAmountPkr` stays `null`; no quote/confirmation service exists (staff-only, not built). `event/page.tsx` surfaces the approved décor/cake/outside-food policy wording, satisfying the runbook's own evidence requirement. Promoted `isTodayOrFutureDate` from `modules/bookings/` to `lib/business/request-window.ts` so events could reuse it without a cross-module import. Added the "Plan a Birthday" CTA to nav/Home. 5 new focused tests mirroring Step 22/21's concurrency shape; 578 total; full `npm run verify` passes including production build.
- [x] Step 24 — protected staff workspace (D-028): `performStaffAssignment` (new, metadata-only orchestrator) and mandatory-reason enforcement added to `lib/domain/staff-transition.ts`; `VersionedStore`/`AppendOnlySink` gained `list()`; `modules/{takeaway,bookings,events}/staff-service.ts` (deps-injected, mirroring `submission-service.ts`) give role-scoped queues, detail+history views, transitions, and assignment for all three entities. `app/staff/` (a third independent root layout) ships a real sign-in/queue/detail UI, English-only and unindexed. Real staff auth (Supabase Auth + MFA) is deferred to Gate 3 — a dev-only credential seam (`modules/staff/dev-credentials.ts` + optional `STAFF_DEV_ACCOUNTS`) stands in, fail-closed by default. AUDITOR can view but not write, per entity. Events' `QUOTED` transition requires `quotedAmountPkr`. 95 new focused tests (reason enforcement, assignment matrix, per-entity concurrent-transition races, AUDITOR view-only, quote requirement, session/cookie isolation, actor/CSRF guard matrix, fail-closed env parsing, `list()`); 658 total; full `npm run verify` passes including production build (all `/staff/*` pages + 13 new `/api/staff/*` routes) and a client-bundle scan; live check confirmed sign-in rendering, fail-closed 500 with `SESSION_SECRET` unconfigured, and a 404 for an unknown entity segment.
- [x] Step 25 — notifications and outbox dispatcher (D-029, Phase 4 complete): `OutboxEvent` gained `id`/`version`; `lib/domain/outbox-store.ts`'s atomic `claimBatch` (due/stale rows, oldest first) plus `markDelivered`/`markRetry`/`markTerminal`; `lib/domain/outbox-dispatcher.ts`'s `runDispatchCycle` (claim → handle → deliver/retry-with-backoff/terminal-at-maxAttempts). One shared outbox singleton (`modules/notifications/deps.ts`) replaces each entity's private sink, matching the single `outbox_events` table. The only handler, `modules/staff/notification-handlers.ts`, upserts by event id into `modules/staff/notification-store.ts` (idempotent), polled via `GET /api/staff/notifications` — Supabase Realtime deferred (D-017; ADR-0007: Realtime is a speed-up, not the guarantee). Trigger: `GET /api/cron/outbox-dispatch`, `Authorization: Bearer $CRON_SECRET`-authenticated (`parseCronSecret` + `lib/security/cron-auth.ts`, fail-closed). 34 new focused tests (worker overlap, crash/retry backoff, poison-message terminal handling, terminal-failure visibility, idempotent handler, cron-auth matrix); 684 total; full `npm run verify` passes including production build and a client-bundle scan; live check confirmed the cron endpoint fails closed (401) with no `CRON_SECRET`/header configured, no regression on `/staff`.
- [x] Step 26 — concierge policy and read tools (D-030, Phase 5 underway): `modules/concierge/policy.ts`'s `CONCIERGE_SYSTEM_POLICY` — one compact, cached, rules-only string sourced from `business/facts.ts`'s existing constants, never a fresh/duplicate copy of approved facts. Four strict read tools (`modules/concierge/tools/`): `getMenu` (reads `getPublishedMenuView`, still `UNPUBLISHED`), `getVenueInfo` (closed topic enum + live hours), `viewCart` (direct re-export of `modules/takeaway/http.ts`'s `getCart` — no second cart implementation), `getRequestStatus` (checks all three request stores, `{found:false}` for both "doesn't exist" and "wrong owner"). `viewCartInputSchema` is deliberately empty — session comes from the server, never the model. No live LLM wiring — no Anthropic SDK dependency exists yet and `ANTHROPIC_API_KEY` has never been read by any code; that's Step 27/29's job. 6 new test files (schema contracts, policy content, per-tool factual-correctness against approved constants); 737 total; full `npm run verify` passes; no new routes this step.
- [x] Step 27 — server-side chat orchestration (D-031): installed `@anthropic-ai/sdk`; `modules/integrations/anthropic-client.ts`'s provider-neutral `ChatClient` + real adapter (never live-called — no API key here; construction never throws, key read lazily inside `sendMessage`). `modules/concierge/orchestrator.ts`'s `orchestrateTurn`: rate-limit → bounded server-held history (never browser-supplied) → tool-call loop bounded by call count/cumulative tokens/wall-clock deadline → any failure mode resolves to the same safe WhatsApp-pointing fallback, never a leaked exception. `system` is always exactly `CONCIERGE_SYSTEM_POLICY`, structurally immune to guest input. `modules/concierge/tool-registry.ts` pairs each tool with a hand-written strict JSON schema; unknown tool name is `NOT_FOUND`, schema-invalid input is a self-correctable tool-result error. No chat UI yet — deferred to Step 28. 4 new test files covering the runbook's evidence bullet exactly (cross-session isolation, prompt-injection guarantees, loop exhaustion, timeout, PII-safe telemetry, cost limit); 779 total; full `npm run verify` passes including production build listing `/api/concierge/chat`; live check confirmed fail-closed 500 with `SESSION_SECRET` unconfigured.
- [x] Step 28 — concierge draft actions and confirmation (D-032): `prepareBookingRequest`/`prepareEventRequest` concierge tools wrap Step 19's existing prepare functions exactly, input schemas reusing `modules/{bookings,events}/schemas.ts`'s exported draft-field validators field-for-field. No `prepareTakeawayRequest` (menu still unpublished, same D-021/D-024 reasoning). No submit tool is ever registered in the tool registry — structurally, not by convention; only a real chat-UI Confirm tap calls `POST /api/{bookings,events}/submit` directly, the exact manual-form endpoints. `orchestrateTurn` gained `pendingConfirmation`, surfacing a successful prepare call's review+token structurally for the UI; only the latest prepare call in a turn wins and it survives a later same-turn failure. Real chat UI shipped (`/concierge`) — CLAUDE.md's "Ask Cladium Concierge" CTA. 4 test files new/extended, including an end-to-end proof a concierge token really submits through the real function and that tampering still fails `STALE_REVIEW`; 805 total; full `npm run verify` passes including production build listing `/[locale]/concierge` and `/api/concierge/chat`; live check confirmed correct rendering and fail-closed behavior.
- [x] Step 29 — agent evaluation harness (D-033, Phase 5 complete): `modules/evals/{types,assertions,runner}.ts` run every case through the real `orchestrateTurn` against a scripted fake `ChatClient` — never a reimplementation. `requiresLiveModel: false` cases run now deterministically (real tool dispatch, including the real Step 19 submission service — e.g. `reviewNeverQuotesAPrice()` checks a genuinely prepared `EventReview`, not case-authored text); `requiresLiveModel: true` cases (bilingual comprehension, ambiguous tool selection, live injection resistance) are visibly `skipped` with a reason, never faked — no `ANTHROPIC_API_KEY` here (D-031). `modules/evals/cases/` covers all 11 runbook-named categories, 27 cases (12 skipped, 15 run and pass, all runnable cases `critical: true`). `tests/unit/evals.test.ts` is the CI gate; `cladium-research/agent/eval-policy.md` documents the threshold/review process. 6 new tests; 810 total (66 files); full `npm run verify` passes; no new route.
- [x] Step 30 — Vapi configuration as controlled artifacts (D-034, Phase 6 underway): `modules/voice/profiles/{types,policy,templates,validate}.ts` — a six-cell `VAPI_PROFILE_MATRIX` (development/preview/production × en/ur) with no assistant id/org id/credential field at all, structurally (same "absence, not convention" pattern as Step 28's missing submit tool); real ids/secrets stay in server env vars, read only by Steps 31–32, not this module. `buildVoiceSystemPrompt` wraps `CONCIERGE_SYSTEM_POLICY` verbatim plus a voice-conduct addendum — never a duplicate copy. `POLICY_VERSIONS`/`TOOL_SCHEMA_VERSIONS` are fingerprint lockfiles implementing "shared policy/tool schema versions" as real drift detection (a test recomputes the live SHA-256 fingerprint and fails on mismatch). `voiceStack` defaults every cell to `PENDING_BAKEOFF` — a real selection needs Step 34's evidence. `firstMessage`/`closingMessage` render English-only in both locales (stricter than `chrome.ts`'s reviewed-UI-copy bar — this is spoken audio, unreviewed by a fluent speaker yet). `docs/vapi-deployment.md` (environment table + deployment procedure + promotion order) and `modules/voice/profiles/CHANGELOG.md` cover the "change logs, and a deployment procedure" wording. 17 new focused tests; 827 total; full `npm run verify` passes; no new route (no live Vapi call yet).
- [x] Step 31 — short-lived browser token service (D-035, Phase 6 underway): `lib/security/jwt.ts` (hand-rolled spec-shaped compact HS256 JWT, `node:crypto` only, no new dependency — a real RFC 7519 wire format since Vapi itself verifies it). `modules/integrations/vapi-client.ts#createVapiTokenIssuer` mirrors `createAnthropicChatClient`'s never-throws-at-construction shape; its claim shape is a best-effort mapping to Vapi's documented format, explicitly flagged as unverified against a live credential. `modules/voice/token/issue-vapi-token.ts#issueVapiToken` is the orchestration — feature-flag check first (`FEATURE_VOICE_EN`/`FEATURE_VOICE_UR`, the first `FEATURE_*` flag actually enforced in this codebase), then a dedicated 5/min rate limit, then issuance; a thrown credential error never leaks its message. `POST /api/vapi/token` reuses Step 20's `parseMutatingRequest` guard unchanged and always embeds the server's own configured origin, never a guest header value. 24 new focused tests; 851 total; full `npm run verify` passes including production build listing `/api/vapi/token`; no live Vapi credential exists here, so only the fail-closed/validation paths are exercised.
- [x] Step 32 — authenticated Vapi tools and webhooks (D-036, Phase 6 underway): `modules/voice/webhook-auth.ts#verifyVapiWebhookRequest` wraps Step 12's `lib/security/webhook.ts#verifyWebhook` unchanged (Vapi-specific header names only, plus a fail-closed wrapper mirroring `cron-auth.ts`'s "unconfigured secret means reject everything"); replay `eventId` is a SHA-256 hash of the raw signed body. `modules/voice/tools/execute-vapi-tool-calls.ts#executeVapiToolCalls` dispatches through the exact same `modules/concierge/tool-registry.ts#dispatchToolCall` text chat uses — never a second tool implementation — reusing Step 19's `runIdempotent` for `toolCallId` idempotency (new scope only), bounded by a 5-call-per-delivery cap (honest per-call rejection for overflow) and an 8s per-call timeout race (`dispatch` is injectable so the timeout path is deterministically testable). `localeForAssistantId` derives locale from our own configured assistant ids (never trusting client metadata); `sessionIdForCall` falls back to a call-scoped synthetic id, never colliding across calls. `/api/vapi/webhook` authenticates identically but only logs the event `type`, doing nothing else yet — no caller needs lifecycle events today. 39 new focused tests; 890 total; full `npm run verify` passes including production build listing both routes; live check confirmed both fail closed (401 unauthenticated, 400 malformed).
- [x] Step 33 — voice web experience (D-037, Phase 6 underway): installed `@vapi-ai/web@2.7.0` and read its real types, confirming (and correcting Step 32's guessed `sessionIdForCall`/schema — the real path is `call.assistantOverrides.metadata`) rather than re-guessing. `voice-call-state.ts` is a pure, fully-tested call-state reducer/error-classifier/duplicate-poll-guard — `voice-panel.tsx` itself is thin real-SDK-wiring verified live, matching this project's no-React-tests convention. `modules/concierge/prepare-tool-result.ts` extracts shared draft-recognition logic out of `orchestrator.ts`; `modules/voice/pending-confirmation-store.ts` + `GET /api/vapi/pending-confirmation` bridge a server-executed voice draft back to the polling browser tab; `pending-confirmation.tsx` extracts the shared review card out of `concierge-chat.tsx` — caught and fixed a real bug in passing (an invented `VOICE_CONCIERGE` source-channel value that doesn't exist in the real schema). `/api/vapi/token` now echoes back `sessionId` (the cookie is HttpOnly). `concierge-mode-toggle.tsx` is a labelled Type/Talk group, only rendered when the locale's voice flag is on server-side. Recording never enabled. 41 new focused tests; 931 total (75 files); full `npm run verify` passes cleanly; live check (English + Urdu) confirmed correct rendering, RTL, disabled-without-CSRF state, and no console errors beyond the expected fail-closed 500s.
- [x] Step 34 — voice quality bake-off (D-038, Phase 6 done for now — 1 deferred): explicitly deferred, not executed, matching Step 18's exact precedent (D-022). `cladium-research/operations/voice-bake-off-plan.md` (new) is the complete, ready-to-run protocol: prerequisites, a three-condition test matrix (quiet indoor/noisy mobile/weak network), a 15-scenario catalogue (every item the runbook names plus no-match recovery and in-call language switch from the localization spec), a scoring rubric with a non-negotiable safety veto (any invented fact disqualifies a profile regardless of other scores), a consent/recording protocol, a session procedure, and a "turning results into code" section. Grounded in real menu items/policy facts throughout, never an invented placeholder. Cannot be executed here: no live Vapi credential/deployed assistants and no real Pakistani speakers exist in this sandbox. No code changed this step.
- [x] Step 35 — click-to-WhatsApp handoff (D-039, Phase 7 underway): `lib/business/whatsapp-link.ts#buildWhatsAppUrl(locale)` is the single hardened link builder for all four click-to-WhatsApp touch points (home, visit, menu-unpublished, and the concierge `getVenueInfo` CONTACT tool) — its `(locale) => string` signature structurally cannot carry guest data, satisfying Gate 8's "avoids exposing customer data in a prefilled URL" by construction. The `?text=` content is a new static, reviewed, bilingual `chrome.ts` entry (`whatsappPrefilledMessage`), not a business/policy claim needing owner-Urdu gating. "Clear consent" is a visible external-navigation notice (`whatsappExternalNoticeText`) beside every link — UX clarity, not a stored consent record (Step 36's job). Bilingual staff-escalation copy extends to the concierge's own safe fallback: `orchestrator.ts`'s English-only `FALLBACK_REPLY`/`ESCALATION_REPLY` became locale-aware `fallbackReply`/`escalationReply` reading two new `chrome.ts` entries with `chrome.ts`'s first interpolation token (`{whatsapp}`). `WHATSAPP_URL`/`WHATSAPP_DISPLAY` (Step 16) unchanged. 2 new test files plus 3 extended; 946 total (78 files); full `npm run verify` passes; live check (English + Urdu) confirmed the correct prefilled URL, notice text, and untranslated brand name.
- [x] Step 36 — consent and privacy controls (D-040, Phase 7 underway): `modules/consent/` — an append-only `ConsentEvent` ledger (`lib/domain/consent-event.ts`, `consent-store.ts`) mirroring `status_events`/`audit_events`, plus one deliberately separate `purgeExpiredBefore` capability for the retention job alone. `consent-service.ts#getConsentSnapshot` defaults `ESSENTIAL_PREFERENCES` to granted (locale/theme keep working, zero guest action) and `META_MARKETING`/`MICROPHONE`/`RECORDING` to not-granted (fail closed); a stale `policyVersion` marks a category for re-consent. `hasConsent` is wired immediately into `issue-vapi-token.ts`, which now rejects `POST /api/vapi/token` with a new `CONSENT_REQUIRED` (403) when MICROPHONE isn't granted — real server-side enforcement, not just a hidden client button. New migration `20260829150001_consent_retention.sql` gives `consent_events` one narrow, auditable DELETE exception (SECURITY DEFINER function, transaction-local flag) since Step 9's trigger otherwise blocks even service_role unconditionally; `status_events`/`audit_events` stay untouched. `GET/POST /api/consent` and `GET /api/cron/consent-retention` (mirrors `/api/cron/outbox-dispatch`) are the new routes. `/privacy` (new) pairs an honest "not published yet" notice (never invented legal content, same pattern as the unpublished menu) with real, working consent controls; `voice-panel.tsx` gained an inline microphone-consent prompt. 4 new test files plus 3 extended; 974 total (84 files); full `npm run verify` passes including a build listing the new routes; live check (English + Urdu, temporary-substitution technique) confirmed the privacy page, fail-closed consent-load error, and the microphone-consent prompt correctly replacing Start Call.
- [x] Step 37 — Meta measurement behind a flag (D-041, Phase 7 underway): `modules/integrations/meta-events.ts#trackMetaEvent` — one flag-then-consent decision point (`FEATURE_META_MARKETING` before `hasConsent('META_MARKETING')`, Step 36's order) every trigger calls through. `metaEventNameSchema` is a closed, custom seven-name catalog matching `production-architecture-v2.md` §11 verbatim, never Meta's own `Purchase`/`Schedule` — "never purchase/booking confirmation" is a type-level guarantee. `meta-client.ts#createMetaEventClient` (real CAPI adapter, never live-called) races a 3s timeout so a slow Meta endpoint can never block the guest request that triggered it; a failed send still returns the `eventId` for the independent browser Pixel channel. Wired into real trigger points: `add_to_cart` (cart-items route), the three `submit_*_request` events (on success only), and `view_menu` (new client `MenuViewTracker` on `menu/page.tsx`). `contact`/`lead` fully built/tested but not click-wired into the WhatsApp links; the actual browser Pixel `<script>`/CSP change also deliberately left unbuilt — both tracked above as separate follow-ups. 3 new test files plus 2 extended; 1000 total (86 files); full `npm run verify` passes including a build listing `/api/meta/track` and a client-bundle scan confirming no credential leak; live check (English + Urdu) confirmed `/menu`/`/book` render unaffected and the view-tracker's CSRF bootstrap fails closed as expected.
- [x] Step 38 — WhatsApp Cloud readiness (D-042, **Phase 7 complete**): `cladium-research/operations/whatsapp-cloud-readiness.md` — readiness report matching Gate 8's checklist verbatim, explicitly distinct from click-to-WhatsApp (Step 35). `whatsapp-client.ts#createWhatsAppCloudClient` — real adapter matching Meta's documented contract, called by nothing (a genuine scaffold, not a wired feature). `whatsapp-webhook-auth.ts` — Meta's real two-secret webhook contract (`X-Hub-Signature-256` raw-body HMAC, distinct from the separate `hub.verify_token` handshake; deliberately its own function, not a forced reuse of Vapi's differently-shaped verifier). `GET/POST /api/whatsapp/webhook` checks `FEATURE_WHATSAPP_CLOUD` first — off means 404. Live check caught a real bug pre-commit: direct `isFeatureEnabled` calls 500'd in this all-flags-unset sandbox instead of 404ing (the same pre-existing gap tracked since Step 36) — fixed with a local `isWhatsAppCloudEnabled()` try/catch wrapper scoped to this one route only. 3 new test files plus 1 extended; 1024 total (88 files); full `npm run verify` passes including a rebuild listing the new route and a client-bundle scan confirming no credential leak; live check re-verified both methods correctly 404 after the fix.
- [x] Step 39 — cross-product E2E and accessibility (D-043, Phase 8 underway): installed Playwright + `@axe-core/playwright`; `tests/e2e/` (11 spec files) covers every dimension the runbook names by name, run against a real `next dev` server fed a synthetic-but-non-secret `webServer.env` (session/CSRF/staff-auth secrets only — `ANTHROPIC_API_KEY`/`VAPI_*`/`META_*`/`WHATSAPP_*` deliberately stay unset, proving the app's own safe-fallback paths for real). Found and fixed 4 real bugs: `isFeatureEnabled` throwing for any unrelated missing flag (fixed at the source; Step 38's one-route workaround removed); `/staff` missing a `<main>` landmark; a WCAG 2.2 AA target-size violation; a real session/CSRF race in `voice-panel.tsx` (two independent mount-time fetches could resolve out of order). Documented, not fixed: `notFound()` under a Suspense-containing catch-all returns HTTP 200 (confirmed via live `WebSearch` as a known upstream Next.js limitation — see the item above); a per-page canonical/hreflang gap (item below). 240/240 E2E tests passing across mobile/tablet/desktop; 1027 unit tests unaffected; full `npm run verify` clean. New `e2e` CI job.
- [x] Step 40 — security and abuse verification (D-044, Phase 8 underway): ran authorization/RLS (offline `check:db-config` re-confirmed 28/28 tables RLS-enabled — no live Postgres here, D-017), CSRF/origin, XSS/injection (confirmed structural: zero `dangerouslySetInnerHTML`/raw `<script>`/`eval` anywhere in `src`), SSRF (confirmed structural: every outbound `fetch` target — Meta CAPI, WhatsApp Cloud — is a fixed `graph.facebook.com` URL with only server-configured path segments, never guest input), request-smuggling assumptions (`request-limits.ts` already never trusted declared `Content-Length`; `origin.ts` already never derived the "expected" side of an origin check from the request itself), webhook spoof/replay (Vapi/WhatsApp, Steps 32/38 — unchanged, re-confirmed), dependency scan (`npm audit`: 0 vulnerabilities, 500 total deps), secret scan, and client-bundle scan (both clean). Found and fixed three real gaps. (1) Only 2 of 34 routes had any rate limit at all — added a shared `lib/http/route-rate-limits.ts` limiter wired into `parseMutatingRequest`'s new optional `rateLimit` option across all 10 guest-mutation routes plus the staff sign-in credential-guessing surface (keyed by attempted staffId, not client IP — an unverified proxy header would be a false sense of protection), proven end-to-end via a new `tests/e2e/rate-limiting.spec.ts` against the real dev server. (2) `lib/security/headers.ts`'s CSP/HSTS/X-Frame-Options/Permissions-Policy/Referrer-Policy — fully built and unit-tested since Step 12 — were never actually wired into any response (`next.config.ts` had no `headers()` at all); fixed by wiring `securityHeaders()` into `next.config.ts` for every non-dev response (skipped in `next dev` — Turbopack's own dev tooling breaks under `X-Content-Type-Options: nosniff`, a `next dev`-only quirk, not an app defect), verified against a real `next build && next start` server: headers present exactly as built, full render/hydration, theme toggle click-tested, no console errors. `'unsafe-inline'` remains in `script-src`/`style-src` as the honest baseline until a separately-scoped nonce migration (needs converting 3 static routes to dynamic rendering — tracked above, not folded in here). (3) Re-verifying with a fresh full E2E run surfaced a real, reproduced cross-component session race in `/concierge` (not caused by fixes 1–2): `ConciergeChat` (default Type mode) fires its own mount-time `GET /api/session/csrf`; a guest tapping "Talk" immediately unmounts it before that fetch resolves (the request itself isn't aborted), while sibling `VoicePanel` mounts and fires its own `GET /api/consent` — under full-suite load the stale response occasionally lands last, silently overwriting the session cookie `VoicePanel` had already captured a CSRF token for, causing its next consent-grant call to fail `403`. The exact class Step 39 fixed *within* `VoicePanel` (D-043), now found *across* it and its sibling. Fixed by lifting the one session/consent-bootstrap fetch to the shared parent `ConciergeModeToggle`, passed down as props to both children (`ConciergeChat`'s other standalone caller, `page.tsx` with voice unavailable, still self-fetches — no sibling exists there to race with). Also fixed in passing: `eslint.config.mjs` gained `playwright-report/**`/`test-results/**` ignores (found live: `npm run lint` failed with 186 errors against Playwright's own minified trace bundle, since ESLint's flat config doesn't read `.gitignore`). 10 new/extended test files (findings 1–2 only — no unit tests for React UI-wiring components, matching this project's established live/E2E-only convention there); 1037 unit tests total (86 files); full `npm run verify` clean; `concierge-shell.spec.ts` passes cleanly 15/15 across all three device projects after the fix; a fresh full E2E re-run confirmed no regression across the whole app.
- [x] Step 41 — performance and resilience (D-045, Phase 8 underway): `tests/unit/performance-load.test.ts` (new, 7 tests) pushes every deterministic in-memory service to hundreds of concurrent operations — 300 distinct guests submitting takeaway requests concurrently (~30–40ms, zero collisions), a 500-event outbox backlog drained across 26 bounded cycles (~30ms, zero loss/duplication), two dispatcher instances racing a 200-event backlog with zero double-delivery, 50 concurrent same-record optimistic-lock attempts (exactly 1 winner), 150 concurrent concierge chat sessions with zero cross-session bleed, and 500 real HMAC-signed Vapi JWTs (~0.1ms/token). Found and fixed a real bug doing exactly this: `lib/security/rate-limit.ts`'s in-memory adapter returned the same mutable window object on every call, so under a genuine `Promise.all` burst every caller read the *final* mutated count — 200 concurrent calls against a 20/min rule let *zero* through instead of the correct 20. Fixed by returning a fresh value snapshot each call. This is the same limiter backing Step 40's guest-route/staff-signin/concierge/Vapi-token rate limits; Step 40's own E2E test never caught it since it used sequential awaited calls, not a true burst. Provider timeout budgets (`TURN_TIMEOUT_MS`=20s, `TOOL_CALL_TIMEOUT_MS`=8s concurrent, `META_EVENT_TIMEOUT_MS`=3s) were cross-checked against Vercel Pro's real documented function-duration limits (confirmed live: 300s default) — 15×–100× headroom, no route overrides `maxDuration`. Core Web Vitals: a genuine local proxy measurement (FCP/LCP ~164ms, CLS 0, four pages) captured via a real Playwright Chromium instance after finding this session's own embedded browser-pane tool never fires Paint Timing/LCP under its rendering path — the real Gate 7 P75 measurement stays scoped to Step 43's staging telemetry, as Gate 7 itself already requires. Realtime interruption: not applicable, feature deliberately unbuilt (ADR-0007/D-017). Proposed alert thresholds (outbox failure rate, rate-limit rejection rate, provider-timeout rate, turn-deadline-hit rate) recorded for Step 43/46 to wire up once a monitoring stack exists. `cladium-research/operations/performance-resilience-report.md` (new) is the full evidence report. 1043 unit tests total (87 files, up from 1037); full `npm run verify` clean; a fresh full E2E re-run scored a completely clean 249/249 across mobile/tablet/desktop.
- [x] Step 42 — backup, restore, and rollback drill (D-046, **Phase 8 complete**): Docker was reachable in this sandbox for the first time since Step 7/10 — ran a genuine local Postgres drill rather than deferring. Found and fixed a real, previously-undiscovered gap: a fresh `db reset` (no restore involved, a pure control test) failed `db:test:rls` outright — every table, `confirmation_tokens`/`idempotency_keys` included, carried full `anon`/`authenticated` CRUD grants via a Supabase platform default `grants.sql` (Step 10) never revoked, undetected through 31 subsequent steps since `db:test:rls` needs a live database this sandbox didn't have until now. Fixed with a new migration (`20260830044140_fix_default_table_privileges.sql`) matching Supabase's own documented remediation (confirmed via live `WebSearch`): `ALTER DEFAULT PRIVILEGES` for all future tables plus `REVOKE ALL`+re-`GRANT` (verbatim from `grants.sql`) for the existing 28 — verified clean via `db:test:rls`/`db:test:schema` post-fix. The backup/restore drill itself took 5 iterations to reach a clean, production-representative method (schema-scope the dump, never strip privileges, don't pre-create the target schema, tolerate one confirmed-benign local-role-hierarchy quirk on `ALTER DEFAULT PRIVILEGES` replay) — each iteration a real, kept finding. Final clean timed run: backup 852ms, full restore 10.1s (all 30 relations), critical test records intact, `db:test:schema`/`db:test:rls` both PASS post-restore. Application rollback: confirmed via live `WebSearch` that Vercel's instant rollback is a routing switch that never touches the database — exactly why this project's zero-destructive-DDL migration discipline (verified across all 13 migrations) is what makes that rollback safe. Proposed RTO 30min/RPO 5min recorded pending owner approval; named operators explicitly left for the owner to assign, not invented. `cladium-research/operations/backup-restore-rollback-drill-report.md` (new) is the full evidence report. 1044 unit tests total (87 files, unaffected — only the migration and its regenerated `database.types.ts` changed); full `npm run verify` clean. **Phase 8 ("full hardening") is now complete — 4/4, Steps 39–42.**
- [x] Step 45 — production readiness decision (D-049, Phase 9 underway): evaluated all 10 `release-gates-v2.md` gates against the full evidence record since Step 1. **Decision: NO-GO** — expected, not a surprise; Gate 0 (owner content/business decisions), Gate 3 (real staff auth/MFA — genuinely unbuilt, the one hard technical blocker, not a flag-deferred capability), and Gate 9 (Pro plan/custom domain/named contacts) all have real open items. Gate 6 (Vapi voice) stands as a model of a clean, correctly-handled deferral by contrast. Three genuinely new findings this step: the Supabase project's Tokyo region has never been checked against Vercel's function region; `npm audit` has no automated CI step (Step 40's check was one-time manual); and the three unenforced request-type feature flags (known since Step 24) were confirmed to be a real Gate 1 failure, not just a nice-to-have, once checked against the gate's literal wording. The report closes with two explicitly separated punch lists — owner/business items this build process cannot supply, and engineering items it can do once scheduled. No application source code changed this step. `cladium-research/operations/production-readiness-decision.md` (new) is the full report.
- [x] Step 44 — owner and staff UAT (D-048, Phase 9 underway): a live collaborative verification pass against the real staging deployment/database rather than independent owner/staff device testing — Claude drove a real browser and a Supabase MCP connection under the owner's real-time direction, checking actual rendered pages/network responses/database rows. Bilingual/RTL, themes, content/menu state, request wording, WhatsApp, and privacy/consent all passed live verification. Operations: submitted a real (clearly fake) booking and event request end to end, both landed correctly in staff queues, walked one through a real `REQUESTED → CONFIRMED` staff transition with correct audit history. Concierge: the real Anthropic integration answered a factual question correctly and correctly declined to guess on a severe-allergy question with a proper WhatsApp hand-off, in English and Urdu. Found and closed a real gap mid-step: no scoped-role (`ORDER_STAFF`) test account existed in staging, so role-scoping itself was untestable — added one to `STAFF_DEV_ACCOUNTS` with the owner's go-ahead, redeployed (confirmed a normal redeploy suffices for server-only env vars, unlike Step 43's `NEXT_PUBLIC_*` gotcha), then verified `order_staff` gets a clean `403` — never a `500` — from bookings/events while keeping full access to takeaway. Voice stays deferred (unchanged, D-038); menu accuracy was correctly reframed as testing the honest not-published-state messaging rather than menu content itself. Owner gave verbal sign-off ("go ahead, record it as approved"); recorded honestly as owner-approved functional verification, not staff self-certification — a genuine independent staff walkthrough remains a tracked follow-up before Step 46. `cladium-research/operations/owner-staff-uat-report.md` (new) is the full evidence report.
- [x] Step 43 — staging release (D-047, **Phase 9 underway**): the first deployment to real, externally-hosted infrastructure — a real Vercel project (Hobby plan, owner's deliberate choice, Pro upgrade tracked for before production) and a real, isolated Supabase project, both created and owned by the business owner. A companion checklist artifact guided the owner through account/project setup and environment-variable configuration. All 13 migrations applied via `supabase db push`; `db:test:schema`/`db:test:rls` both re-run live against real hosted Postgres and passed. A third-party "AI API gateway" relay was considered for the Anthropic key and rejected after direct investigation (no `baseURL` override exists in the code; the relay's test keys returned `401`; its own recommended setup was a blind `irm | iex` script execution, independently blocked by Claude Code's own safety classifier) — a real Anthropic key was obtained instead, no code changed. Found and fixed a real deployment gotcha: the first deploy left every mutating request failing closed with `403` because Vercel's cache-reusing "Redeploy" didn't pick up a changed `NEXT_PUBLIC_APP_URL` — root-caused via direct testing (a request with the exact correct Origin header and CSRF token still rejected, ruling out anything client-side) and fixed with a genuinely fresh rebuild. Full live smoke-test pass: real booking and event requests submitted end to end against real Postgres; double-click idempotency proven at the real-database level (identical `requestId` both times); staff sign-in and both queues correct; outbox dispatcher manually triggered, 3/3 delivered with correctly PII-free notification payloads; WhatsApp link and consent grant/revoke both correct (verified via network log); every disabled-flag route fails safely, never a `500`; and — for the first time in this entire project — a real Anthropic API call returning a correct, factually accurate, on-brand reply. No application source code changed this step (the first entirely clean `src`/`tests` diff). `cladium-research/operations/staging-release-report.md` (new) is the full evidence report.

- [x] Real staff authentication with structurally-enforced owner/manager MFA (D-050, Phase 9 follow-up work, closes Step 45's one hard technical blocker from D-049): `modules/integrations/supabase-auth-client.ts`/`supabase-admin-client.ts` (provider-neutral, ADR-0008, verified against `@supabase/supabase-js`'s real shipped types before writing any code, the same discipline Step 33 applied to `@vapi-ai/web`), `modules/staff/supabase-directory.ts` (real `StaffDirectory`, fail-closed to `null` on any error including full misconfiguration), `modules/staff/supabase-credentials.ts` (the orchestration — an OWNER/MANAGER account can never reach `SIGNED_IN` without a verified TOTP factor, unconditional on role, not on what Supabase's own `nextLevel` says), `modules/staff/mfa-session.ts` (a short-lived signed cookie bridging the two legs of sign-in/enrollment, reusing Step 31's real JWT signer), two new routes (`POST /api/staff/mfa/enroll`, `POST /api/staff/mfa/enroll/verify`), and a real sign-in UI in `app/staff/dashboard.tsx` (toggled, never mounted alongside the dev form, so the existing E2E suite needed no changes). Additive, not a cutover: `createCompositeStaffDirectory` lets dev and real accounts coexist safely (no id-space collision), so nothing in staging broke on deploy. 1087 unit tests total (91 files, up from 1044); full `npm run verify` passes including a production build listing both new routes and a client-bundle scan confirming the service-role key never reaches the browser. Real account provisioning is the owner's remaining action — see the two follow-up items above and `cladium-research/operations/real-staff-account-setup.md`.

- [x] Fixed a real bug found live in staging while verifying D-051 (D-052): `GET /api/takeaway/cart` could be statically pre-rendered at build time, freezing its feature-flag response until a genuinely fresh rebuild — the same underlying class of bug as Step 43's `NEXT_PUBLIC_APP_URL` trap (D-047), applied to a route handler's output instead of a client-bundle constant. It was the only `GET` route whose flag-check branch could return before touching a cookie/header, so it was the only one exposed. Fixed with `export const dynamic = 'force-dynamic'` on that one route. Worth checking any future `GET` route with an early-return path against this same trap.
- [x] Wired the three unenforced request-type feature flags into their guest-facing routes (D-051, Phase 9 follow-up work, closes a real Gate 1 failure from Step 45/D-049): `lib/http/mutating-route.ts#parseMutatingRequest` gained an optional `featureFlag` parameter, checked first — before content-type, before session resolution — matching `errors.ts#featureDisabled`'s "a disabled feature must not confirm it exists" reasoning. All 8 mutating routes across takeaway/bookings/events now pass the matching flag; `GET /api/takeaway/cart` (the one non-`parseMutatingRequest` route in the group) got the identical check inlined. The concierge's own read tools (`viewCart`/`getMenu`) are deliberately unaffected — they call service functions directly, never through these HTTP routes. `tests/unit/mutating-route.test.ts` (new) covers the shared option's full contract. 1092 unit tests total (92 files); full `npm run verify` passes clean. No live staging toggle test — staging already runs all three flags `true`, so this deploy is a no-op there by design.

## Update rule

Keep only actionable work here. Move completed items to the short Completed list; put rationale in `DECISIONS.md`, not in task descriptions. After any completed runbook step, refresh the step-completion snapshot (overall/phase, as percentages — not effort) in `PROJECT_STATE.md`'s `## Progress` section; see `CLAUDE.md` Workflow rules for the rule itself.
